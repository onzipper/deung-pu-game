# Agent ground rules (repo root)

## Next.js

Next.js 16 (App Router). Its API may differ from your training data.

- **NEVER** Read/Grep/Glob inside `node_modules/**` — no exceptions.
- If unsure about a Next.js API: ask the owner, or read the ONE file path the brief gives you.
- Prefer checking `src/app/**` for existing patterns in this repo over external docs.

Entry point for all agents: `AI.md` · full shared rules: `docs/agent-rules.md`
