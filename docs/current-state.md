# Current state

> กติกา: อัปเดตไฟล์นี้ทุกรอบ · block ที่ถูก supersede → `docs/history/` · สั้นพออ่านได้ทุก session

_Last updated: 2026-07-12_

## Where we are

**P0 (Engine Foundation Vertical Slice) complete — pending owner visual review + PR** (tech §19 · `docs/design/deungpu_P0_SCOPE_LOCK_v1.md`, branch `feat/p0-engine-foundation`). P0-01→P0-11 implement เสร็จตามที่บันทึกด้านล่าง; **P0-12 P0 Handoff Check ตรวจผ่านครบ 12/12 ข้อของ P0 Done Definition** (P0_SCOPE_LOCK §6) ด้วยหลักฐานจริง (qa-specialist audit, ไม่แก้ production code): `npm test` 272 เขียว (20 ไฟล์), `lint`/`build`/`tsc -p server/tsconfig.json --noEmit` เขียวทั้งหมด, GET `/game` → 200 (บน dev server จริง), live 2-client proof (colyseus.js script ชั่วคราว รันแล้วลบ ไม่ commit): join map+channel เดียวกัน → roomId ตรงกัน + เห็นกัน 2 entries ใน state.players + move sync ข้าม client + leave → entity หายจาก state จริง; client ที่ 3 คนละ channel ("CH.2") → roomId ต่างกัน + state แยกกันสนิท. Scope audit (ข้อ 12): grep ทั่ว `src/`+`server/` หา keyword ระบบนอก scope (login/inventory/market/guild/PvP/monetization ฯลฯ) เจอเฉพาะ TODO comment ชี้ P1 (โอเคตามกติกา) ไม่มี implementation จริงหลุดเข้ามา; dependencies ใน `package.json` ตรวจแล้วสมเหตุผล (pixi/colyseus/colyseus.js/tsx เท่านั้นที่เพิ่มจาก base Next.js). ข้อที่เป็น visual (2,3,4,5,9,10) verified by tests เท่านั้น — **visual จริงบนตายังต้องรอ owner review ตอน PR**. รอ owner: (a) เปิด browser ดูจริง (b) เปิด PR `feat/p0-engine-foundation` → `develop`

### วิธีรัน realtime local (2 terminal)
- Terminal 1: `npm run dev:server` (Colyseus บน ws://localhost:2567; env `PORT` override ได้)
- Terminal 2: `npm run dev` (Next client; env `NEXT_PUBLIC_RT_URL` override server url ได้)
- เปิด 2 browser tab ที่ `/game` → เห็นผู้เล่น 2 ตัว (local=เหลือง, remote=ฟ้า) ขยับ sync กัน
- ไม่ start server = /game ยังเล่น solo (net.status = "offline", log warning)

## Latest work (P1 เริ่มแล้ว — World Sync)

- 2026-07-12: **P1-03 Server-side mob simulation เสร็จ** (branch `feat/p1-world-sync`, TA §18/§6/§11) — ย้าย mob จาก client-local (P0-09) เป็น **server-authoritative**. **Pure core (server+offline ใช้ร่วม, ไม่มี pixi):** `src/game/mob/ai.ts` (aggro/leash/pull cap §18.3 + AI LOD §6/§11 + respawn timer — decision fn เดี่ยว ๆ เทสต์ได้) · `src/game/mob/simulation.ts` (`createMobSimulation` authoritative stateful: spawn ชุดแรก [spawnAllPockets เดิม] + respawn ทีละตัวเมื่อ `killMob` [respawnDelayMs, clock inject, ≤ activeCap] + AI state machine wander↔chase↔return [reuse stepWander/stepMovement] + LOD active/asleep). **AI state machine:** wander (aggro เช็คก่อนเดิน → chase ถ้ามี player ในรัศมี+cap ไม่เต็ม) → chase (stepToward player; leash เมื่อ target หาย/ลากเกิน leashRadius/หนีเกิน deaggroRadius → return) → return (stepToward spawnOrigin; ถึง → wander). pull cap ต่อผู้เล่นสร้างใหม่ทุก tick + จองสิทธิ์ทันทีกัน over-pull. **Server glue:** `MapRoom` สร้าง sim + `setSimulationInterval(ai.tickHz 10Hz)` → ป้อน player pos → `sim.tick` → เขียน `state.mobs` (MapSchema `MobState{mobId,mobType,tx,ty,state,hp}`) + `MSG_DEBUG_KILL_MOB` handler (ทดสอบ death→respawn ก่อน P1-05). **Client:** refactor `mob/manager.ts` → **view manager** (render/interpolate ผ่าน buffer P1-01 เหมือน remote player, facing derive จาก delta ไม่ sync ทิศ) + `app.ts` **mob source controller** (online=server callbacks / offline=รัน sim ตัวเดียวกัน local ป้อน view — graceful fallback, สลับตาม net status) + combat stub retarget มอน server, **เล่นแค่ effect ไม่ฆ่า** (TODO P1-05). **knob ใหม่ (config, PENDING OWNER tune):** MobAiConfig (tickHz 10/chaseSpeed 2.4/aggroRadius per type/leashRadius 8/deaggroRadius 9/pullCap 10 [§18.3 8–12]) + MobLodConfig (aoiRadius 14/idleTickHz 2) + respawnDelayMs 5000 (override ต่อ pocket ได้). **AOI filter §18.2:** ยังไม่บังคับ (30 CCU/map เล็ก) — จุด filter ชัดเจน + TODO ใน `MapRoom.syncMobsToState`. **Proof จริง (colyseus.js, รันแล้วลบ ไม่ commit):** 8/8 PASS — server spawn 12 มอน · เดินเข้าใกล้ → มอน aggro ไล่ (dist 1.70→0.02) · หนีลงใต้ (คอลัมน์ tx=13 เดินได้) → มอนถูกลากออก pocket (ty→11.23) → leash → return กลับเข้า pocket area (15.42,5.68) · debug kill → หายจาก state → respawn id ใหม่ใน 5s. (หมายเหตุ: รอบแรก flee ทับ blocked tile → server P1-02 validation ตี move กลับจริง = ยืนยัน collision ทำงาน). เกต: **337 tests** (2 ไฟล์ใหม่: game-mob-ai, game-mob-simulation) + lint + build + `tsc -p server/tsconfig.json` เขียว. ถัดไป: **P1-04 skill schema loader (§50.1)**.
- 2026-07-12: **P1-02 Server-authoritative movement เสร็จ** (branch `feat/p1-world-sync`) — server เลิก trust ตำแหน่ง client, validate จริง (TA §6/§7/§16.3). ใหม่: `src/shared/movement-validation.ts` (pure validateMove: non_finite→teleport→speed→blocked, clamp elapsed กัน clock skew) + `MSG_POSITION_CORRECTION` (net-protocol) + `MovementValidationConfig` (config knob, mirror client/server). `server/rooms/MapRoom.ts` onCreate โหลด map (loader เดิม) + reuse engine collision (snapToTile+isWalkableTile) + อ่าน knob จาก `DEFAULT_ENGINE_CONFIG` (**single source of truth — ไม่ copy สูตร**) → validate ทุก MSG_MOVE, ผิด→ไม่ apply + ส่ง correction กลับ (per-player MoveTracker + cooldown). Client: `net-client` รับ correction (นับ correctionCount → debug overlay) → `app.ts` hook → `local-player.applyCorrection()` snap-only reconcile. **Share strategy**: server import engine pure modules ผ่าน `@/` alias — tsx resolve paths จาก `server/tsconfig.json` ได้ (พิสูจน์แล้ว, ไม่ลาก pixi เข้า bundle). Proof (scratchpad, ไม่ commit): client ปกติเดินไม่โดน correction · client โกงส่ง teleport → โดน snap กลับตำแหน่ง valid. เกต: 304 tests + lint + build + `tsc -p server/tsconfig.json` เขียว.
- 2026-07-12: **P1-01 Netcode interpolation เสร็จ** (branch `feat/p1-world-sync`) — remote player ยกจาก lerp ง่าย (P0-07) เป็น **snapshot interpolation buffer** (render ย้อนหลัง bufferMs=120): `src/engine/net/interpolation.ts` (pure ring buffer + sampleAt, out-of-order=drop, starved=extrapolate สั้น+clamp maxExtrapolationMs=100, เกิดใหม่=clamp ไม่ลากจากไกล) + refactor `remote-player-manager.ts` (push+sample, inject clock) + `NetInterpolationConfig` (config knob) + `tests/engine-net-interpolation.test.ts` (15 เทสต์). local ยัง full client-predict — reconcile hook วาง TODO ไว้รอ **P1-02**. เกต: 287 tests + lint + build เขียว.

## Older work

- 2026-07-12: **P1 Balance Proposal v1** (deep-worker, docs-only ไม่แตะโค้ด) — `docs/design/proposals/deungpu_P1_BALANCE_PROPOSAL_v1.md` สถานะ PROPOSAL/PENDING OWNER (ไม่ใช่ spec จนกว่าเคาะ, เข้า spec ผ่าน §59.4). เสนอ: ค่า **k=50** (range 30–80, anchor: DEF=k → มิติเกชัน 50%) + ตาราง damage ตัวอย่าง; **stat baseline นักดาบ lv1–10** (10 stats §15.1) + secondary; **มอน Map 1** ดึ๋งปุ๊/หมูพอง/elite/boss (HP·ATK·DEF·tierReduction §15.5); **ตาราง skill 5 อาชีพ** ตาม field §50.1 เป๊ะ (นักดาบ 4 skill full YAML, อีก 4 อาชีพ 3 skill draft table — ใช้ชื่อ skill จาก §17.6 map เข้า 3 branch §8); guardrail check §48.9 (farming แพ้ boss single-target ~3.8×, AoE ไม่ one-shot elite ต่ำสุด 7 hit, maxTargets 6 ตาม §56.4). **จุดต้องเคาะ (checklist ท้ายไฟล์)**: k, stat, mob, ตาราง skill, + 2 spec-gap — resourceCost มีใน §50.1 แต่ 10-stat ไม่มี resource pool (P1 รัน cooldown-only ได้), damageType physical/magic ยังไม่ split resist
- 2026-07-12: **P0-12 P0 Handoff Check** (qa-specialist audit, ห้ามแก้ production code) — ตรวจ P0 Done Definition ครบ 12/12 ข้อ PASS ด้วยหลักฐานจริง: gate ทั้งหมดเขียว (`npm test` 272/272 ผ่าน 20 ไฟล์, lint สะอาด, `next build` สำเร็จ, `tsc -p server/tsconfig.json --noEmit` สะอาด); live check GET `/game` → 200; เขียน proof script ชั่วคราว (colyseus.js, วางใน repo root ชั่วคราวตาม known-traps tsx-resolve-trap, รันแล้วลบทันที ไม่ commit) พิสูจน์ข้อ 7/8 จริง: 2 client join mapId+channelId เดียวกัน → roomId เท่ากัน, state มี mapId/channelId/roomId ถูกต้อง, players.size=2 หลัง join ทั้งคู่, move sync ข้าม client เห็นจริง, leave → players.size ลด (entity หาย); client ที่ 3 ใช้ channelId="CH.2" → roomId ต่างจาก CH.1 สนิท, state แยกกันจริง (isolate). ข้อ 2/3/4/5/9/10 (visual) ยืนยันจาก unit tests ที่มีอยู่แล้ว (engine-iso-*, engine-render-*, engine-movement-direction, engine-animation-manifest, game-mob-spawn/wander, game-combat-hit-test) — mark "verified by tests, visual จริง = owner review ตอน PR" (ไม่มี jsdom/WebGL render test ใน P0). ข้อ 12 (scope): grep `src/`+`server/` หา login/inventory/market/guild/PvP/monetization ฯลฯ — เจอเฉพาะ TODO comment ชี้ P1 ทั้งหมด ไม่มี implementation จริง; `package.json` dependencies ตรวจแล้วสมเหตุผลกับ P0 scope. Docs consistency: CODEMAP เทียบกับ `find src server -type f` ตรงกันครบ (ไม่มีไฟล์ตกหล่น/เกิน), `tests/docs-guard.test.ts` ผ่านอยู่แล้วใน gate. **ผลสรุป: P0 complete, pending owner visual review + PR** — ไม่พบ bug/ความไม่ตรง spec ระหว่างตรวจ, ไม่ต้องแก้ production code
- 2026-07-12: **P0-11 Debug Overlay** — P0_SCOPE_LOCK §4.10 (fps · player tile/world coord · mapId · roomId · channelId+status · entity count · pointer tile · depth-sort debug toggle). **Pure**: `src/engine/runtime/debug-info.ts` (`EngineDebugInfo` shape + `buildDebugInfo()` ปัด fps เป็นจำนวนเต็ม + playerTile/pointerTile 2 ตำแหน่งทศนิยม ที่เดียว + `IDLE_NET_DEBUG_INFO` fallback เมื่อ `net.enabled=false`) · `src/ui/debug-overlay-logic.ts` (`DebugOverlayState{visible,depthDebug}` + `isDebugToggleKey`("F3")/`toggleVisible`/`toggleDepthDebug` reducer, แยกจาก component ให้เทสต์ได้). **Engine glue**: `src/engine/runtime/app.ts` เพิ่ม pointer tile tracking (`pointermove`/`pointerleave` บน `app.canvas`, แปลง client coord → canvas-relative (`getBoundingClientRect`) → ลบ `scene.world.position` → `screenToTile`+`snapToTile` — canvas CSS px กับ `scene.world`/`viewport` อยู่ space เดียวกันเพราะ `autoDensity=true`) + `EngineHandle.getDebugInfo()`/`setDepthDebug()` (passthrough ไป scene). `src/engine/render/scene.ts` เพิ่ม `entityCount` getter (จาก `DepthRegistry.size`) + `setDepthDebug(enabled)`: สร้าง `depthDebugLayer` (sibling ของ `entityLayer` ใน `world`, วาดทับบนสุด, ไม่ใช้ zIndex ร่วมกับระบบ depth sort จริงกัน conflict) — เปิด = สร้าง/sync `Text` แสดง depth rank เหนือทุก entity ทุกครั้งที่ `syncDepth()` resort (dirty-gated เหมือนเดิม), ปิด = ลบ label ทั้งหมดทันที (ไม่มี label ค้าง ไม่กระทบ perf ตอนปิด). `src/engine/config.ts` เพิ่ม `DebugOverlayConfig` (defaultVisible/pollIntervalMs=250/depth label color·fontSize·offsetY, shallow-merge ใน `createEngineConfig`). **UI**: `src/ui/DebugOverlay.tsx` ("use client") — panel มุมจอ poll `getDebugInfo()` ทุก `pollIntervalMs` ผ่าน `setInterval` (**ไม่ subscribe ทุก frame**, ตาม tech §2), รับ `getHandle: () => EngineHandle | null` (accessor function ไม่ใช่ handle ตรง ๆ) ทนกรณี engine ยัง init ไม่เสร็จ/ถูก destroy; แสดงครบทุกค่าตาม scope + ปุ่ม toggle depth debug (เรียก `setDepthDebug`) + ปุ่ม/คีย์ลัด F3 ซ่อน-แสดง panel (`preventDefault` กัน browser). `src/ui/GameCanvas.tsx` เก็บ `EngineHandle` ใน `useRef` (**ไม่ใช่** `useState` — กัน world-state-เข้า-React-state) + render `<DebugOverlay>`. FPS text เดิมในแคนวาส (per-frame, ticker) **คงไว้ตามเดิมไม่ย้าย** — overlay แสดงค่า poll เฉลี่ยแยกต่างหาก พอสำหรับ debug (ตัดสินใจนี้ document ไว้ตรงนี้ตามที่ brief อนุญาตให้เลือกเอง). เทสต์ใหม่: `tests/engine-runtime-debug-info.test.ts` (roundTile + buildDebugInfo shape/rounding + IDLE_NET_DEBUG_INFO), `tests/ui-debug-overlay-logic.test.ts` (isDebugToggleKey + toggle reducer pure/ไม่ mutate), เพิ่มเคสใน `tests/engine-config.test.ts` (DebugOverlayConfig default+override) — รวม 272 เทสต์เขียว, lint เขียว, build เขียว. **ไม่ทำ**: Zustand bridge จริง (P1 — จด TODO ในโค้ด/docs), React component render test (ไม่มี jsdom WebGL — ตาม brief), depth-sort visual QA ด้วยตา (ปล่อยให้ owner/QA ลองที่ `/game` กด toggle เอง)
- 2026-07-12: **P0-10 Combat Stub** — chain input→animation→hit→feedback (dummy เท่านั้น, P0_SCOPE_LOCK §4.9). **Pure logic**: `src/game/combat/hit-test.ts` — `findHits(attackerPos, facing, targets, tileSize, shape)`: ระยะ = euclidean บน **tile coords** ตรง ๆ, arc = มุมรวม (องศา) รอบทิศ facing แต่เทียบเป็น **มุมบนจอ** (project ทั้ง facing ผ่าน `directionToScreenUnit` และ target-vector ผ่าน `tileToScreen` ของ delta แล้ว `atan2(-sy,sx)` สูตรเดียวกับ `resolveDirection`) เพราะ diamond 64×32 ไม่ใช่สี่เหลี่ยมจัตุรัส มุม tile ดิบจะเบี้ยวไม่ตรงกับที่ตาเห็น; ระยะ 0 เป๊ะถือว่าโดนเสมอ (มุมไม่มีความหมาย) — เหตุผลเต็มอยู่ header comment ไฟล์. เพิ่ม `rollDummyDamage`/`advanceCooldown`/`canAttack`/`applyDummyDamage` (hp/death transition, pure — mob/manager.ts เป็นคน apply ผลลง instance จริง) + helper `screenAngleForDirection`/`tileUnitVectorForScreenAngle` (reuse วาด hitbox debug wedge ให้ตรงเกณฑ์ hit จริงเป๊ะ ไม่ใช่วงกลมเดา). **Pixi glue**: `src/game/combat/damage-number.ts` (`createDamageNumberLayer` — Text ลอยขึ้น+fade ผ่าน scene entity API เดิม [addEntity/removeEntity, zLayer 50 สูงสุด], สร้าง/ทำลายตรง ๆ ต่อ hit — TODO(P1) BitmapText+pool ตาม tech §11) · `src/game/combat/combat-stub.ts` (`createCombatStub` — cooldown gate (config) → `player.triggerAttack()` → `findHits` กับ `mobs.getAliveTargets()` → `mobs.applyDamage()` (dummy roll) + `damageNumbers.spawn()`, hitbox debug = wedge polygon sample จาก `tileUnitVectorForScreenAngle` ตาม arc/radius จริง fade ตาม config, zLayer 40). **Engine เปลี่ยน**: `src/engine/input/keyboard.ts` เพิ่ม `ATTACK_KEY` ("Space") edge-triggered `consumeAttackPressed()` (กันกดค้างสแปม, ไม่กระทบ MOVE_KEYS เดิม) · `src/engine/player/local-player.ts` เพิ่ม `triggerAttack()`/`isAttacking`/`consumeAttackPressed()` (ล็อก animation="attack" จนจบคลิปตาม `attackFrameDuration×attackFrames` แล้วกลับ idle/walk เอง, เดินระหว่างโจมตีได้ตำแหน่งขยับปกติ) · `src/game/mob/manager.ts` เพิ่ม hp ต่อ mobType (จาก `combat.mobHp`), `applyDamage()`/`getAliveTargets()`, death feedback (squash แนวตั้ง + fade บน `animator.view` ตรง ๆ ไม่ชน mirror scale.x) แล้ว despawn จริงหลัง `deathFeedback.durationMs` (ไม่ respawn ใน P0) · `src/engine/config.ts` เพิ่ม `CombatStubConfig` (attack shape radius/arcDegrees/cooldownMs, dummyDamage range, mobHp per type + defaultMobHp, hitboxDebug enable/color/alpha/durationMs, damageNumber style/timing, deathFeedback — ทุกค่า Design Knob ห้าม hardcode) · wire เข้า `src/engine/runtime/app.ts` (update หลัง mobs.update, destroy ก่อน mobs.destroy). เทสต์ใหม่ `tests/game-combat-hit-test.test.ts` (259 เขียวรวม): 8 ทิศ hit/miss (halfArc >45° กันชน float-tie) + boundary near-tie แยกเทสต์ (44°/46° รอบ facing, pattern เดียวกับ `engine-movement-direction.test.ts`) + หลังผู้เล่นไม่โดน + ระยะ 0 โดนเสมอ + ระยะเกิน radius พลาดแม้ตรงหน้าเป๊ะ + rollDummyDamage ในช่วง (หลาย seed, deterministic, min=max ไม่เรียก rng) + cooldown gate เต็มรอบ + hp/death transition (รวม damage เกิน hp → ติดลบได้ไม่ clamp). lint/build เขียว. **ไม่ทำ** (P1, ตาม P0 §4.9): full damage formula (multiplicative diminishing, tech §15.2), skill schema จริง (GS §50.1), item drop/EXP/gold, boss mechanic, mob respawn, damage number pooling เต็มระบบ (TODO ชี้ tech §11 ในโค้ด)
- 2026-07-12: **P0-09 Dummy Mob Pocket Spawn** — ไฟล์แรกใน `src/game/**` (game logic บน engine, ใช้ engine ผ่าน public API เท่านั้น). **Pure logic**: `src/game/mob/rng.ts` (RngFn inject ได้ — `defaultRng`=Math.random runtime, `createLcgRng` seeded deterministic ในเทสต์) · `src/game/mob/spawn.ts` (`spawnPocketMobs`/`spawnAllPockets` — TA §18.1 fixed pocket + random point inside: จำนวน = random(packSize.min..max) clamp ด้วย activeCap, จุดเกิด = `findWalkableSpawnPoint` สุ่มใหม่ถ้าโดน blocked จำกัด `maxPlacementAttempts` รอบ แล้ว best-effort ข้ามถ้าหาไม่เจอ — ไม่ throw) · `src/game/mob/wander.ts` (`createWanderState`/`stepWander` — สลับ idle/walk สุ่มช่วงเวลาจาก `MobWanderConfig`, เดินจริงใช้ `stepMovement` เดิม [reuse ฟรี, ไม่เขียน collision ใหม่] + **leash แบบง่าย**: ผูก pocket.area เป็นเงื่อนไข block เพิ่มใน predicate ที่ส่งเข้า stepMovement → มอนไถลติดขอบเหมือนชนกำแพง ไม่หลุด area). **Pixi glue**: `src/game/mob/manifest.ts` (reuse `AnimationManifest`/`resolveClip` ของ engine — เลือกวาดจริงแค่ 2 ทิศ [S,N] + mirror 6 ทิศ เพราะ mob dummy symmetric ไม่มี asymmetry ซ้าย-ขวาแบบ player, ทิศยังคำนวณ+ส่งเข้า animation จริงเผื่อ P1 ใส่ art ไม่สมมาตร) · `src/game/mob/placeholder.ts` (`generateMobTextures` — Graphics→RenderTexture ต่อ mobType: slime=ก้อนเขียวเด้ง squash-stretch, mushroom=หมวกแดง+ก้าน; generate **ครั้งเดียวต่อ mobType** แชร์ข้าม instance กัน GPU เปลือง) · `src/game/mob/manager.ts` (`createMobManager` — spawn ทุก pocket ตอนสร้าง, ticker update wander+animator+moveEntity, destroy แยก texture ออกจาก instance เพราะแชร์กัน [ห้ามเรียก `animator.destroy()` ต่อตัว — จะ destroy texture ของตัวอื่นที่ยังใช้อยู่]). เพิ่ม `MobConfig` (spawn/wander/animation/defaultStyle/styles) ใน `src/engine/config.ts` (Design Knob ทั้งหมด, ไม่ hardcode) + wire เข้า `src/engine/runtime/app.ts` (spawn ตอน createEngine, update ใน ticker, destroy ตอนปิด engine). เทสต์ pure ใหม่ 3 ไฟล์ (`tests/game-mob-{rng,spawn,wander}.test.ts`, 245 เขียวรวม): RNG determinism, จำนวน spawn ในช่วง packSize/clamp activeCap, จุดเกิดอยู่ใน pocket.area และเดินได้จริง (หลาย seed + P0_TEST_FIELD จริง), wander interval/speed มาจาก config (ไม่ hardcode), leash ไม่หลุด area (deterministic + seeded random หลายร้อย step, ทั้งกรณี block ด้วย area อย่างเดียวและ block ด้วย map จริงร่วมด้วย). lint/build เขียว. **TODO จดในโค้ด (P1)**: server-authoritative spawn/AI (TA §18.1/§7), object pooling (tech §11 — P0 มอนไม่ตายเพราะยังไม่มี combat จึงยังไม่ pool, แต่โครง `Map<id, MobInstance>` ไม่ขวางเพิ่มทีหลัง). **ไม่ทำ** (ตาม scope P0 §4.8): loot/EXP/aggro/combat (P0-10), respawn, mob AI จริง
- 2026-07-12: **P0-08 Channel Stub** — channelId เป็น first-class field: `src/shared/net-protocol.ts` `JoinOptions.channelId` (client ส่งจริง, default constant `DEFAULT_CHANNEL_ID` ไม่ hardcode ซ้ำฝั่ง server) + `src/engine/config.ts` `NetConfig.channelId` (Design Knob ใหม่, default `DEFAULT_CHANNEL_ID`) + `runtime/app.ts` ส่ง channelId ใน joinOptions จริง. **Server**: `server/index.ts` ผูก `gameServer.define(MAP_ROOM_NAME, MapRoom).filterBy(['mapId','channelId'])` (Colyseus 0.16 `RegisteredHandler.filterBy` — ดึงค่าจาก client joinOptions ตรง ๆ ตอน matchmake/create, ไม่ต้องพึ่ง metadata แยก); `MapRoom.onCreate` อ่าน `options.channelId ?? DEFAULT_CHANNEL_ID`; roomId จริง (`this.roomId`) เข้า state — มีอยู่แล้วตั้งแต่ P0-07 (ยืนยันซ้ำ). **Client debug API**: `net-client.ts` เพิ่ม `getNetDebugInfo(): {status, mapId, roomId, channelId, playerCount}` (playerCount คำนวณผ่าน pure helper ใหม่ `computePlayerCount` ใน `sync.ts`, online = remoteCount+1 มิฉะนั้น 0) ให้ P0-11 debug overlay ใช้แทนอ่าน `status` ดิบ. **Manual proof (ชั่วคราว, รันแล้วลบ ไม่ commit)**: start `dev:server` จริง → colyseus.js client A1 join channel "CH.1", A2 join channel "CH.1" (map เดียวกัน), B1 join channel "CH.2" (map เดียวกัน) → ผล: A1.roomId === A2.roomId (same-channel = same room, PASS), A1.roomId !== B1.roomId (diff-channel = diff room, PASS), `state.roomId` ตรงกับ `room.roomId` จริงของ Colyseus ทั้ง 3 client (PASS) — proof PASSED ทั้งหมด. เทสต์ pure ใหม่: `tests/engine-net-sync.test.ts` (JoinOptions.channelId shape + computePlayerCount 6 เคส) + `tests/engine-config.test.ts` (NetConfig.channelId default + override). รวม 222 เทสต์เขียว (14 ไฟล์), `npm run lint` เขียว, `npm run build` เขียว, `tsc -p server/tsconfig.json --noEmit` เขียว. **ไม่ทำ** (P1, tech §6/RUNTIME §4): auto-assign channel จริง, UI เลือก channel, party sync, channel capacity
- 2026-07-12: **P0-07 Realtime Room Skeleton** — Colyseus (matched 0.16 line: `colyseus@0.16.5` + `@colyseus/schema@3` server · `colyseus.js@0.16.22` client, schema 3 ทั้งคู่ = wire-compatible) + `tsx` dev runner. **Server** (`server/`, แยก process ตาม L4, own tsconfig legacy-decorators): `index.ts` (Server+define+listen), `rooms/MapRoom.ts` (onCreate/onJoin/onMessage move/onLeave — P0 trust position), `schema/MapRoomState.ts` (PlayerState{tx,ty,direction,anim} + MapRoomState{mapId,channelId,roomId,players}). **Shared** `src/shared/net-protocol.ts` (wire contract, ไม่มี runtime dep). **Client** `src/engine/net/{sync,net-client,remote-player-manager}.ts` (pure throttle/coerce + colyseus.js glue graceful-offline + remote entity lerp สีต่าง) + `config.ts` NetConfig + `runtime/app.ts` wire net เข้า ticker (EngineHandle.net สำหรับ P0-11) + `local-player.ts` expose animation + `GameCanvas.tsx` env url. **พิสูจน์จริง**: start server + 2 colyseus.js client → A/B อยู่ room เดียวกัน, B เห็น A spawn (3,3), A ขยับ→B เห็น (7.5,4.25) dir=E anim=walk, A leave→B ได้ onRemove; channelId=CH.1. เทสต์ pure `tests/engine-net-sync.test.ts` (216 เขียวรวม), lint/build เขียว (server แยกออกจาก Next build/lint). **ยังไม่ทำ (P1, จด TODO ในโค้ด)**: reconnect 30s grace (tech §6/§59.1), server-authoritative movement validation (tech §6), channel auto-assign+party sync (RUNTIME §4), persistence, deploy Render
- 2026-07-12: **P0-06 Sprite Animation Prototype** — data-driven animation (tech §17.4, L15): `src/engine/animation/manifest.ts` (pure `resolveClip`: logical 8-dir → sprite source + mirror flag · 5 ทิศวาด S/SW/W/NW/N + mirror SE←SW/E←W/NE←NW · รองรับ 8-dir override = ประกาศครบ 8 ไม่ mirror · `advancePlayhead` pure frame timing/loop · `createPlayerAnimationManifest`) + `src/engine/animation/player-placeholder.ts` (generate placeholder texture ด้วยโค้ด Graphics→RenderTexture — ตัวละคร asymmetric [accent แดงข้างเดียว] ให้เห็น mirror ด้วยตา, walk/idle/attack, foot anchor คงที่) + `src/engine/animation/animator.ts` (เล่นเฟรมบน Sprite, mirror = scale.x=−1 รอบ anchor.x=0.5 → เท้าไม่ย้าย) + `config.ts` `PlayerAnimationConfig`/`PlayerSpriteStyle` + `local-player.ts` ใช้ animator (walk/idle ตาม intent, ถอด nose ออก). เทสต์ `tests/engine-animation-manifest.test.ts` (37 เคส: 8 ทิศ×idle/walk + 8-dir override + error + timing/loop). รวม 204 เขียว, lint/build ผ่าน. **ยังไม่ทำ**: art จริง (ใช้ placeholder), mob animation wire (P0-09), attack input (P0-08), server sync (P0-07)
- 2026-07-12: **P0-05 Local Player Movement Prototype** — `src/engine/input/keyboard.ts` (intent tracker: WASD+arrows → tile-space intent ผ่าน inverse projection · pure `intentFromKeys`) + `src/engine/movement/{mover,direction}.ts` (pure: `stepMovement` เดินต่อเนื่อง+normalize เฉียง+collision slide แยกแกน+clamp dt · `resolveDirection` tile→8-dir จากมุมบนจอ + `directionToScreenUnit`) + `src/engine/player/local-player.ts` (pixi glue: keyboard→mover→direction→scene entity + camera follow + placeholder body/nose บอกทิศ) + config `PlayerConfig` (speed 4 tile/s, maxStepSeconds 0.1) + `runtime/app.ts` wire player เข้า ticker (dt วินาที). **ถอด debug pointer entity ออกจาก scene.ts** (player แทน) + ลบ `debugPointerEntity`/`debugEntity` config. เทสต์ใหม่ 3 ไฟล์ (167 เขียวรวม), lint/build ผ่าน. **ยังไม่ทำ**: sprite animation จริง (P0-06), click-to-move/pathfinding (P1), server sync (P0-07)
- 2026-07-12: P0-04 Renderer Scene Graph & Depth Sorting — `src/engine/render/{depth-registry,camera,scene}.ts` + `src/engine/runtime/app.ts` (rewrite) + config theme/camera knobs + `tests/engine-render-{depth,camera}.test.ts`. แยก pure logic (DepthRegistry = source of truth ของลำดับ + dirty tracking · camera math = bounds/clamp/lerp) ออกจาก pixi glue (scene.ts). Sort: assign unique zIndex rank จาก DepthRegistry → pixi sort เฉพาะเมื่อ dirty (ไม่ sort ทั้ง scene ทุก frame, TA §11); tie-break depthKey→tx→insertion seq (total order deterministic). Ground layer วาดครั้งเดียว (ไม่ sort). Camera: fixed iso no-rotation, lerp follow + clamp ขอบ map. **Cross-review fix (BLOCKER-1)**: lock convention "tile ที่ส่งเข้า entity/prop API = foot ต่อเนื่อง → `render/placement.ts` `entityFootToScreen` = tileToScreen (ไม่ +0.5)"; basis เดียวกับ depthKey/camera (กันเหลื่อมครึ่ง tile กับ cursor/กล้อง); prop integer ใน p0-test-field ปรับเป็น n+0.5 (author intent = กลาง cell); เพิ่ม known-traps 2 entry. เทสต์ 116 เขียว, lint/build ผ่าน
- 2026-07-12: P0-03 Test Map Config — `src/engine/map/{types,loader,p0-test-field}.ts` + `tests/engine-map-loader.test.ts`. MapConfig ตาม spec P0 §4.3 (field ชั้นนอกล็อก); CollisionLayer = blockedRects+blockedTiles → build blockedSet (packTile, lookup O(1) จาก integer tile); PropSpawn (tile float ได้ + zLayer สำหรับ depth band); MobPocket (area rect + packSize + activeCap ตาม TA §18). loader validate ด้วยมือ ไม่ใช้ zod (fail-loud, ชี้ field ผิด). P0 Test Field = 24×24, spawn (12.5,12.5), กำแพง+บ่อน้ำ block, 7 props (บาง float), 3 pockets (slime/mushroom). pure TS ไม่พึ่ง React/pixi
- 2026-07-12: P0-02 Isometric Coordinate System — `src/engine/iso/coords.ts` (TilePoint/ScreenPoint + tileToScreen/screenToTile/snapToTile) + `src/engine/iso/depth.ts` (depthKey + zLayer band) — pure math, ห้าม render/PixiJS; เทสต์ round-trip + band non-overlap (never-downgrade zone)
- 2026-07-11: สร้าง Next.js project + push ขึ้น GitHub
- 2026-07-11: นำ spec 6 ไฟล์เข้า repo (game spec v14 canonical + map bibles + tech architecture v1.4 + decision locks)
- 2026-07-11: ตั้งระบบ docs-for-AI (AI.md, CODEMAP, feature-map, context packs, decision-index, known-traps, path-guard test, agent personas)
- 2026-07-11: เพิ่ม specialist personas (engine/game/ui/qa/docs-curator) — realtime/worker/data/audio จดเป็น deferred รอ phase จริง (`.claude/README.md`)
- 2026-07-11: owner เคาะ 3 decisions: branch model (develop), ClickUp tracking, tech ตั้งเลข balance + update spec (ดู decision-index)
- 2026-07-11: นำภาพ ref 11 ภาพเข้า `docs/design/art-reference/` — กลุ่ม A (pixel art) = style target, กลุ่ม B = layout เท่านั้น
- 2026-07-12: owner สั่ง**เลิกใช้ ClickUp** — track งานใน repo ที่เดียว (docs); ไม่ลบข้อมูลใน ClickUp (decision-index #13/#16 superseded)
- 2026-07-12: import **spec v15 + tech v1.5 + P0_SCOPE_LOCK_v1**; ย้าย v14/v1.4 → `docs/history/`; อัปเดต reference ทุก index doc (canonical ชี้ v15/v1.5) — delta หลัก = P0 Scope Lock
- 2026-07-12: **P0-01 Runtime Foundation** — ติดตั้ง pixi.js v8.19 + สร้าง engine runtime (`src/engine/config.ts`, `src/engine/runtime/{app,resize,assets}.ts`) + `src/ui/GameCanvas.tsx` + route `/game`; unit test config+resize เขียว, build ผ่าน

## Blockers / owed

1. ~~ยังไม่มีโค้ดเกม / hold~~ — **owner สั่งเริ่ม P0 แล้ว 2026-07-12** — **P0-01→12 เสร็จหมดแล้ว 2026-07-12** (P0-12 handoff check ตรวจผ่าน 12/12) — รอ owner visual review บน browser + เปิด PR `feat/p0-engine-foundation` → `develop`
2. ตัวเลข balance P1 (ค่า k, stat baseline, ตาราง skill 5 อาชีพ, mob Map 1) — **proposal ร่างแล้ว รอ owner เคาะ**: `docs/design/proposals/deungpu_P1_BALANCE_PROPOSAL_v1.md` (PENDING OWNER; ใช้ค่า draft ใน config ไปก่อน ไม่ block implement, เข้า spec ผ่าน §59.4). Checklist สิ่งที่ต้องเคาะอยู่ท้ายไฟล์ — จุดสำคัญ: k=50, resource pool (§50.1 มี resourceCost แต่ 10-stat ไม่มี pool), damageType physical/magic
3. ~~ClickUp skill~~ — **ปิดแล้ว 2026-07-12** (เลิกใช้ ClickUp)
4. ~~PixiJS 8 ยังไม่ได้ติดตั้ง~~ — **ติดตั้งแล้ว 2026-07-12** (pixi.js v8.19, P0-01)
5. ภาพ ref กลุ่ม B (04–11) เป็น painterly — owner อาจ gen ใหม่เป็น pixel art มาแทน (ยังไม่กระทบ P0 — P0 ใช้ placeholder sprite generated ด้วยโค้ดทั้งหมด)
6. Colyseus server P0 เป็น local dev เท่านั้น (`npm run dev:server`, ยังไม่ deploy Render) — ตาม decision-index #20 พัฒนา local ต่อจนกว่า owner เคาะ deploy

## Owner decisions affecting immediate work

- Spec-first rule: ห้ามเดา ห้ามคิดเอง — เกิน spec ต้องอัปเดต spec ก่อน (decision-index #1)
- Locked decisions ทั้งหมด: tech architecture §0.1 (L1–L18) — server-authoritative, MySQL, Render, iso, 5 อาชีพ, P0 scope lock ฯลฯ

## Do not touch right now

- `docs/design/**` + `docs/tech/**` — spec แก้ได้เฉพาะ owner เคาะ

## Next recommended work

- **รอ owner ก่อน**: visual review P0 บน browser จริง (ข้อ 2/3/4/5/9/10 ของ Done Definition ยัง verified by tests เท่านั้น) + เปิด PR `feat/p0-engine-foundation` → `develop` (ยังไม่ merge เอง — ต้องยืนยันกับ owner ก่อนเสมอตาม AI.md)
- ตาม decision-index #20 (ขยาย run ถึง P1 โดยไม่รอ merge P0): แตก branch ซ้อน `feat/p1-world-sync` จากหัว P0 — ร่าง P1 issue breakdown ลง docs ก่อนเริ่มโค้ด (tech ร่าง, owner ดูตอน review PR ที่สอง)
- ระวังตอนต่อ P1 World Sync: net layer P0 ยัง trust position + ไม่มี reconnect/validation/auto-assign channel จริง — TODO ชี้ tech §6/§59.1/RUNTIME §4 จดไว้ในโค้ด (`server/rooms/MapRoom.ts`, `net-client.ts`)
- **P1 HUD**: ติดตั้ง Zustand จริง + ย้าย pattern poll ชั่วคราวของ P0-11 (`src/ui/DebugOverlay.tsx`) เข้า store bridge ตาม `docs/context/ui.md` (contract: UI คุยกับ game ผ่าน Zustand เท่านั้น)
