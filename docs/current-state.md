# Current state

> กติกา: อัปเดตไฟล์นี้ทุกรอบ · block ที่ถูก supersede → `docs/history/` · สั้นพออ่านได้ทุก session

_Last updated: 2026-07-12_

## Where we are

**P1 (World Sync) complete — pending owner review + PR** (tech §12 · `docs/tech/deungpu_P1_ISSUE_BREAKDOWN_v1.md`, branch `feat/p1-world-sync`, บนหัว `feat/p0-engine-foundation`). P1-01→P1-11 implement เสร็จตามที่บันทึกด้านล่าง; **P1-12 P1 Handoff Check ตรวจผ่าน 12/13 ข้อ PASS + 1 ข้อ PARTIAL ของ P1 Done Definition** (`deungpu_P1_ISSUE_BREAKDOWN_v1.md`) ด้วยหลักฐานจริง (qa-specialist audit, ไม่แก้ production code): gate ทั้งหมดเขียว (`npm test` 568/568 ผ่าน 39 ไฟล์, lint สะอาด, `next build` สำเร็จ, `tsc -p server/tsconfig.json --noEmit` สะอาด). Live E2E รวม (colyseus.js in-process server พอร์ต 2599 ชั่วคราว รันแล้วลบ ไม่ commit, ~8 รอบ iterate แก้ script เอง ไม่แตะ production code): 2 client join เห็นกัน (players.size=2) → เดินปกติไม่โดน correction → speed-hack (จั๊มไกล) → snap กลับจริง (reason=teleport) → เดินเข้า pocket → มอน aggro ไล่ (มีรันที่เห็น distance ไล่ชัดเจน 2.28→1.80→1.08→0.60→0.12→0.12 ทุกครั้งที่ไม่ติดกำแพงในเส้นทาง) → หนีไกล → มอนเลิกไล่ (deaggro/leash) → cast `sword_basic_slash` → `skill_result` dmg=11 ตรง hand-calc เป๊ะ (ATK12×baseMult1.0×[50/(50+4)]=11.11→11, §15.2) → ตีตายจริง (despawn จาก state) → respawn ใน 5s (สังเกตได้ตรง `respawnDelayMs` knob) → force-close connection (unconsented) → reconnect ภายใน grace 6s (env override) → sessionId/roomId/ตำแหน่งเดิมกลับมาเป๊ะ → party 2 client (partyId เดียวกัน) → channel/room เดียวกันอัตโนมัติ (แยกจาก solo room) → เดิน test-field → exit เหนือ → `MSG_MAP_TRANSITION`→map1 → เดิน map1 → north gate → `MSG_MAP_TRANSITION`→city-hub → cast ในเมือง → `MSG_CAST_REJECTED` reason=`safe_zone`. **ข้อ 4 (mob aggro) = PARTIAL**: chase สำเร็จชัดเจนในบางรัน แต่ scripted-approach บางรันเจอ mob ติดกำแพง (tx6 column, p0-test-field) ทำให้ระยะไล่ plateau กลางทาง — เป็นข้อจำกัดของ script (เส้นทาง auto-approach ชนกำแพง) ไม่ใช่บั๊กเกม (สอดคล้องกับ `tests/game-mob-simulation.test.ts` ที่ยืนยัน aggro/leash/chase แบบ deterministic seeded อยู่แล้ว + มีอย่างน้อย 1 รันสด PASS ชัดเจนไร้กำกวม). ข้อที่เป็น visual/perf ล้วน (1 smooth interpolation, 9 touch, 12 FPS@40mobs+300dmg#) = verified by tests/structure เท่านั้น (ไม่มี headless WebGL renderer วัด FPS อัตโนมัติ) — **owner กด F3+F4 ที่ `/game` ตอน review ยืนยันเอง**. Scope audit (ข้อ 13): grep `src/`+`server/` หา save/inventory/market/enhancement/guild/PvP/HoF/monetization ฯลฯ — เจอเฉพาะ field-name ที่มาจาก skill schema จริง (§50.1 `pvpModifier`) กับ prop label "กิลด์นักล่า"/"ถนนร้านตีเหล็ก" (ชื่อ landmark เฉย ๆ ไม่มีระบบจริง) — ไม่มี implementation ระบบนอก P1 หลุดเข้ามา; balance ทุกตัวอยู่ใน `CombatBalanceConfig`/`warrior-skills-server.ts` (PENDING OWNER header ครบ, ชี้ proposal doc) ไม่มี hardcode ใน `formula.ts`/`MapRoom.ts`. Docs consistency: CODEMAP/feature-map ตรงไฟล์จริง (`tests/docs-guard.test.ts` เขียว). **ไม่พบ bug ที่ต้องแก้ระหว่างตรวจ** — รอ owner: (a) เปิด browser ทดสอบจริง (F3/F4) (b) review PR `feat/p0-engine-foundation` (P0, ค้างมาจากรอบก่อน) + เปิด PR ใหม่ `feat/p1-world-sync` → `develop` (c) เคาะ pending questions ใน `deungpu_P1_ISSUE_BREAKDOWN_v1.md` (ตาราง skill 5 อาชีพ+k, ลำดับอาชีพถัดจากนักดาบ, city hub ตัด P2 หรือไม่ — ตอนนี้ implement ไปแล้วตาม draft)

### วิธีรัน realtime local (2 terminal)
- Terminal 1: `npm run dev:server` (Colyseus บน ws://localhost:2567; env `PORT` override ได้)
- Terminal 2: `npm run dev` (Next client; env `NEXT_PUBLIC_RT_URL` override server url ได้)
- เปิด 2 browser tab ที่ `/game` → เห็นผู้เล่น 2 ตัว (local=เหลือง, remote=ฟ้า) ขยับ sync กัน
- ไม่ start server = /game ยังเล่น solo (net.status = "offline", log warning)

## Latest work (P1 World Sync — complete, pending owner review)

- 2026-07-12: **P1-12 P1 Handoff Check** (qa-specialist audit, ห้ามแก้ production code) — ตรวจ P1 Done Definition (`deungpu_P1_ISSUE_BREAKDOWN_v1.md`) **12/13 PASS + 1 PARTIAL**. Gate: `npm test` 568/568 (39 ไฟล์), lint สะอาด, `next build` สำเร็จ, `tsc -p server/tsconfig.json --noEmit` สะอาด. Live E2E รวม (colyseus.js in-process server พอร์ต 2599 ชั่วคราว วางที่ project root ตาม known-traps tsx-resolve-trap, ~8 รอบ iterate debug script เอง — ไม่แตะ production code แม้แต่บรรทัดเดียว, รันแล้วลบทันที): (1) 2 client join p0-test-field → เห็นกัน players.size=2 roomId ตรงกัน PASS. (2) เดินปกติ (step ≤ speed cap) → ไม่โดน correction PASS. (3) speed-hack (jump 1 update ไกลเกิน teleportThresholdTiles=3) → `MSG_POSITION_CORRECTION` reason=**teleport** + ตำแหน่งไม่ขยับจริง PASS. (4) mob aggro/leash: เดินเข้าใกล้ pocket-slime-north → มอนไล่ (มีรันที่เห็นชัด distance 2.28→1.80→1.08→0.60→0.12→0.12 ทุก 250-300ms) → หนีไกลเกิน deaggroRadius/leashRadius → มอนเลิกไล่จริง PASS 4b เสมอ, **4a (chase) = PARTIAL**: บางรัน scripted-approach เข้าใกล้กำแพง tx6 (p0-test-field) ทำให้มอนที่ไล่ถูก collision กันจน plateau กลางทาง ก่อนบรรจบ 0 — เป็นข้อจำกัดของ auto-approach ใน proof script (เลือกเป้าใกล้แนวกำแพงพอดี) ไม่ใช่บั๊ก server (ai.ts/simulation.ts มี `tests/game-mob-ai.test.ts`+`tests/game-mob-simulation.test.ts` ยืนยัน aggro/leash/chase deterministic seeded อยู่แล้ว, และมีอย่างน้อย 1 รันสดที่ไม่ติดกำแพงแสดง chase ไหลลื่นเต็มรูปแบบ). (5)(6) cast `sword_basic_slash` → `MSG_SKILL_RESULT` dmg=11 ตรง hand-calc เป๊ะ (ATK12×baseMult1.0×[k/(k+DEF)]=12×1.0×50/54=11.11→11, §15.2, ทำซ้ำได้หลายรัน) PASS. ตีตายจริง → mob หายจาก `state.mobs` (despawn) → respawn ใน `respawnDelayMs`(5000ms)+margin (สังเกตจำนวน slime กลับมาเท่าเดิม) PASS. (7) force-close connection (unconsented, ไม่ใช้ `room.leave()`) → รอ 1.2s (ใน grace 6s ที่ override ผ่าน `RECONNECT_GRACE_SECONDS` env) → `client.reconnect(token)` → sessionId/roomId/ตำแหน่งเดิมกลับมาเป๊ะ (ตรงกับ §59.1 decision table + `tests/shared-reconnect.test.ts`) PASS. (8) party 2 client (partyId เดียวกัน สุ่มใหม่ทุกรัน) → auto-assign channel/room เดียวกัน (แยกจาก solo room) PASS. (9) เดิน test-field → exit เหนือ (tx10-13,ty0-1) → `MSG_MAP_TRANSITION` targetMapId=map1 → rejoin ที่ targetSpawn PASS. (10) เดิน map1 → north gate (tx18-22,ty0-1) → `MSG_MAP_TRANSITION` targetMapId=city-hub → rejoin PASS. (11) cast ใน city-hub (zoneType=safe) → `MSG_CAST_REJECTED` reason=**safe_zone** (ปฏิเสธก่อนทุก reason อื่นตาม §14) PASS. **ข้อที่เป็น visual/perf ล้วน (1 smooth interpolation, 9 touch, 12 FPS@40mobs+300dmg#)**: verified by tests/structure เท่านั้น (ไม่มี headless WebGL renderer วัด FPS อัตโนมัติ) — **owner กด F3 (debug overlay) + F4 (stress harness) ที่ `/game` ตอน review ยืนยันด้วยตาเอง**. **ข้อ 13 (balance = config, ไม่ hardcode)**: grep `src/game/combat/**`+`server/rooms/MapRoom.ts` หา numeric literal — เจอเฉพาะ geometry/dev constant (`HITBOX_DEBUG_ZLAYER=40`, `HITBOX_ARC_SEGMENTS=16`, unit conversion `*1000`) ไม่ใช่ balance; ค่า balance ทั้งหมดอยู่ใน `DEFAULT_COMBAT_BALANCE_CONFIG`(k=50/stat)+`warrior-skills-server.ts` มี header **PENDING OWNER** ชี้ `docs/design/proposals/deungpu_P1_BALANCE_PROPOSAL_v1.md` ครบ; `graceSeconds=30` ใน config = ค่าจาก spec §59.1 ตรง ๆ (ไม่ใช่ knob รอเคาะ) PASS. **Scope audit**: grep ทั่ว `src/`+`server/` หา save/inventory/market/enhancement/guild/PvP/HoF/monetization — เจอเฉพาะ (ก) `pvpModifier` = field ชื่อจริงใน skill schema §50.1 (ตัวคูณ, ไม่ใช่ระบบ PvP) (ข) prop label "กิลด์นักล่า"/"ถนนร้านตีเหล็ก" ใน city-hub.ts = ชื่อ landmark เฉย ๆ (comment บอกชัดว่า NPC/ร้านค้าจริง = P2+) — **ไม่มี implementation ระบบนอก P1 หลุดเข้ามา**. Docs consistency: CODEMAP/feature-map ตรงไฟล์จริง (`tests/docs-guard.test.ts` เขียวอยู่แล้วใน gate). **ไม่พบ bug ที่ต้องแก้ระหว่างตรวจ ไม่มีการแก้ production code**. **สรุป: P1 complete, pending owner F3/F4 visual review + PR `feat/p1-world-sync` → `develop`** (ถัดจาก PR P0 ที่ยังค้าง review). ถัดไป: owner review ทั้ง 2 PR + เคาะ balance proposal (k/skill table 5 อาชีพ) + ตัดสินใจว่าจะเริ่ม P2 อะไรก่อน.
- 2026-07-12: **P1-11 City Hub พื้นฐาน เสร็จ** (branch `feat/p1-world-sync`, GS §3.3 live hub · §14 Safe Zone · TA §6 · never-downgrade: iso coordinate — reuse pure + เทสต์คุม). **เมืองหลัก "นครอรุณผนึก"** (ชื่อ **locked GS §3.3 §173** — brief พิมพ์ "พระอรุณคซึก" ผิด, spec-first → ใช้ชื่อ locked) `src/engine/map/city-hub.ts` (`CITY_HUB_ID="city-hub"`): 32×32 กลาง, `zoneType:"safe"`, spawn = ลานกลางเมือง, **mobPockets ว่างเปล่า** (ไม่มี combat), landmark = props placeholder (วิหารผนึก/ถนนร้านตีเหล็ก/กิลด์นักล่า/จุดประกาศข่าวนักล่า — ชื่อจริงใน comment ตาม GS §3.3 "เขตในเมือง") + แนวอาคาร/กำแพงเมือง collision + ประตูใต้. **Zone flag** = optional `zoneType:"safe"|"field"` ใน MapConfig (default "field", loader validate enum, `MapZoneType` นิยามใน config.ts) — ไม่ sync ผ่าน wire (client/server derive จาก registry เดียวกัน). **Safe zone (GS §14)**: `validateCast` เพิ่ม reason **`safe_zone`** (เช็คก่อนทุก reason — เมืองปฏิเสธ cast ทุกกรณี), `MapRoom.handleCast` ส่ง `map.zoneType` เข้า validate (server-authoritative) + client `combat-stub` เพิ่ม `combatEnabled` gate (safe → กด Space/แตะมอน = no-op) wire ผ่าน `app.ts` `combatAllowed=map.zoneType!=="safe"`. **Room capacity ต่อ map**: knob ใหม่ `net.cityHubCapacity=80` (TA §6 "ไม่มี combat → cap สูงกว่า ~80–100") + pure `soloChannelCapacityForZone(zone,ch,city)` → `MapRoom.onCreate` เลือก cap ตาม zone (safe=cityHub/field=channel, env `CITY_HUB_CAPACITY` override) — โหลด map ก่อนคำนวณ maxClients. **Exits** (bible: เมืองอยู่เหนือ Map 1): Map 1 ประตูเหนือ → **city-hub** (แทน dev wire ไป test field เดิม), city-hub ประตูใต้ → map1 (ครบวง); test field คง dev exit → map1 (dev boot map). **เกต: 568 tests (39 ไฟล์) + lint + build + `tsc -p server/tsconfig.json` เขียวหมด**. **Proof จริง (colyseus.js in-process server port 2599, รันแล้วลบ ไม่ commit): 3/3 cases PASS** — (1) 2 client join city-hub → เห็นกัน (players.size=2, roomId เดียวกัน, `create city-hub CH.1 cap=80`); (2) cast_skill ในเมือง → `MSG_CAST_REJECTED` reason=**safe_zone**; (3) เดินจากลานกลางลงประตูใต้ → `MSG_MAP_TRANSITION` targetMapId=**map1** @(20.5,5.5). **ไม่ทำ (จด scope — P2+)**: NPC (9 ตัว GS §3.3), ระบบร้านค้า/market/blacksmith, event เมือง (GS §149), 8-zone detailed layout (03-main-city-sheet), art production. ถัดไป: **P1-12 handoff**.
- 2026-07-12: **P1-10 Map 1 production layout + map transition เสร็จ** (branch `feat/p1-world-sync`, GS §57.3 · MAP_LAYOUT_BIBLE §Map 1 · MAP_SCALE §Map 1 · never-downgrade: iso coordinate correctness — pure + เทสต์คุม). **Map 1 config** `src/engine/map/map1.ts` (`MAP1_ID="map1"`): 40×40 **Small–Medium** (MAP_SCALE §1/§5), N ประตูเมือง/warp + กำแพงเว้นช่องประตู, spawn ใต้ประตู + safeCamp (Safe Point เดียว §5), **4 farming pocket ตาม MAP_SCALE §6** (สไลม์ C cap18 / นกจิกปุ๊ E cap12 / หมูป่า SW cap18 / Elite S cap1 — pack/activeCap เป๊ะจากตาราง, respawn = midpoint ของช่วง), props/collision = placeholder (reuse style เดิม; boss = phase-based ไม่ใช่ farming pocket → out of scope). **Map registry** `src/engine/map/registry.ts` (pure, ใช้ร่วม client+server): mapId→MapConfig build ครั้งเดียว fail-loud + `validateMapCrossRefs` (exit.targetMapId มีจริง + targetSpawn เดินได้ในปลายทาง) + getMap/requireMap/hasMap/buildMapRegistry(fixture). **Transition = separated rooms + loading/fade (§57.3, ไม่ seamless)**: `MapExit`{exitId,area,targetMapId,targetSpawn} + `findExitAt`/`isTileInRect` (pure, `types.ts`) + loader parse exits (intrinsic; cross-ref = registry). **Server** `MapRoom`: โหลด map จาก registry ตาม mapId (ไม่ hardcode) + `checkExit` ทุก MSG_MOVE valid → เพิ่งเข้า exit ยิง `MSG_MAP_TRANSITION` (tracker.lastExitId กัน spam) — server บอกทาง, client re-join (separated). **Client** `app.ts` refactor เป็น **outer shell + per-map `mountWorld(map,spawn)`** (teardown สะอาด: mob view/entities/net handlers/listeners) + `requestTransition` → `transition-state.ts` (pure fade/lock state machine idle→fadeOut→[SWAP]→fadeIn→idle, tested) + `transition.ts` (pixi fade overlay บนสุด) → จอมืดสุด destroy world เดิม (net leave=consented, client อื่นเห็นหาย) → mount world ปลายทางที่ targetSpawn → fade in, **input lock** ระหว่างนั้น. **online = server authority (onMapTransition) · offline = client ตรวจ findExitAt เอง** (mirror pure fn — ไม่มี server ก็ข้าม map local ได้). Knob ใหม่ `TransitionConfig` (fadeColor/fadeOutMs/fadeInMs). P0 Test Field เพิ่ม dev exit ขอบเหนือ → map1 (ครบวงกับ map1-north-gate). **วิธีลอง**: (browser) เดินไปขอบเหนือ test field ตรง exit (tile 10–13, แถวบนสุด) → จอ fade → โผล่ Map 1 ที่ประตู; เดินขึ้นผ่านประตู Map 1 (tile 18–22 แถวบน) → กลับ test field. (proof จริง) 2-client script: A เดินเข้า exit → roomId เปลี่ยน + mapId=map1, B ยังอยู่ห้องเดิมเห็น A หาย, A เดินกลับ → กลับ test field — **ทุก assertion PASS** (server แยกพอร์ตกัน 2567 dev, colyseus.js, รันแล้วลบ ไม่ commit). **เกต: 558 tests (39 ไฟล์) + lint + build + `tsc -p server/tsconfig.json` เขียวหมด**. **ไม่ทำ (จด scope)**: art production จริง (ตอนนี้ placeholder เหมือน P0), stat/style ต่อ mob type ใหม่ (bird/boar/boar_elite = fallback defaultMob/defaultStyle — balance = owner §48/§59.4), minimap (§57.4 launch = level design), Map 2+ (bible มี layout แต่ยังไม่แปลง). **จุดที่ bible/spec ไม่ครอบ → tech ตัดสิน (report owner)**: (1) tile dimension เป๊ะ (bible = "ไม่ใช่ coordinate spec") → 40×40; (2) พิกัด zone ต่อ tile = แปลงทิศ bible ตรง ๆ; (3) respawnDelayMs = midpoint ของช่วง §6 (owner tune จริง); (4) Map 1 ไม่มีทิศเชื่อม Map 2 ชัดใน bible — dev wire N gate กลับ test field เพื่อ proof (production = city hub/Map 2). ถัดไป: **P1-11 City hub พื้นฐาน** (CityHubRoom, TA §6 — หมายเหตุ: TA §12 note ว่าตัดไป P2 ได้ถ้า owner อยากเร่ง combat loop).
- 2026-07-12: **P1-09 Click-to-move + pathfinding + touch เสร็จ** (branch `feat/p1-world-sync`, TA §17.3 · L11 · never-downgrade: iso coordinate correctness — pure + เทสต์คุม). **A\* (pure)** `src/engine/pathfinding/astar.ts`: บน integer iso grid, 8-dir neighbors (diagonal **กัน corner cutting** — เดินเฉียงได้เฉพาะเมื่อ ortho ข้างเคียงทั้งสอง walkable), octile heuristic, MinHeap open set (array คู่ขนาน + closed-set skip stale = ไม่ decrease-key), `maxSearchNodes` cap (เกิน→null, map ใหญ่ไม่ค้าง frame), walkable ผ่าน callback (`isWalkableTile` เดิม, ไม่ผูก MapConfig) — คืน waypoints integer cell (ไม่รวม start), `[]`=ถึงแล้ว, `null`=เดินไม่ถึง/goal block/เกิน cap. **Path-follower (pure)** `src/engine/movement/path-follower.ts`: `advancePathFollower` เดินตาม waypoints ด้วย `stepMovement` **ตัวเดียวกับ WASD** (ความเร็ว/collision เดียวกัน → server validate P1-02 ผ่านเหมือนกัน) — เล็งกลาง cell (n+0.5, convention foot ต่อเนื่อง), ถึง (arrivalRadius)→waypoint ถัดไป, จบ→arrived, dynamic obstacle (target block / stuck ชนกำแพงคั่น)→blocked (คืน signal ให้ controller ไม่ replan เอง = แยก calc ออกจาก decision). **Controller** `local-player.ts`: `moveTo(goal)`→A*→เดินตาม + marker จุดหมาย fade (cosmetic knob); **WASD กดแทรก = manual override ชนะเสมอ ยกเลิก path**; คลิกใหม่=replan; dynamic block→`replanToGoal` (config `replanOnBlock`, replan **เฉพาะตอน blocked event** ไม่ทุก frame); walk-to-attack helper `faceToward`/`requestAttack` (รวมช่อง Space → cooldown gate เดียว)/`isFollowingPath`/`cancelPath`; applyCorrection ยกเลิก path ด้วย (server=truth). **Touch/click (L11)** `app.ts`: `pointerdown` (mouse ซ้าย button 0 + touch tap) → `footFromEvent` (foot ต่อเนื่อง ไม่ +0.5 ซ้อน) → แตะมอนในรัศมี `clickMobPickRadius`=walk-to-attack (ถึง `attackRange`→requestAttack เท่ากับ Space, ไกล→moveTo แล้ว `evaluateWalkToAttack` ทุก frame จนถึงระยะ/มอนหนี), แตะพื้น→`moveTo` (A*). **Knob ใหม่** `PathfindingConfig` (maxSearchNodes 4096 / arrivalRadius 0.4 = speed·maxStep / replanOnBlock true / clickMobPickRadius 0.9 / marker style+fade) — Design Knob ครบ. **วิธีทดสอบ (owner ใน browser + touch device)**: คลิกพื้น→เดินอ้อมกำแพง/บ่อน้ำ, คลิกมอน→เดินเข้าไปตี, WASD ระหว่างเดิน path→ยกเลิกทันที, มือถือ tap เดิน/tap มอนตี. เทสต์ใหม่ pure: `tests/engine-pathfinding-astar.test.ts` (ortho/diagonal/อ้อมกำแพง/null 3 กรณี/corner-cut ห้ามมุด+มุมโล่งเดินได้/cap nodes) + `tests/engine-movement-path-follower.test.ts` (ถึง waypoint/หลายจุด/heading/blocked target+stuck/ยกเลิก). **เกต: 533 tests (37 ไฟล์) + lint + build + `tsc -p server/tsconfig.json --noEmit` เขียวหมด**. **ไม่ทำ (จด scope)**: mobile polish เต็ม (virtual joystick, HUD ปุ่มสกิล, gesture) = P2; click ขวา = สงวน P2. ถัดไป: **P1-10 Map 1 production layout + transition** (GS §57.3 · MAP_LAYOUT_BIBLE).
- 2026-07-12: **P1-08 Channel auto-assign + party sync เสร็จ** (branch `feat/p1-world-sync`, GS §59.3 · TA §6). **กลไก auto-assign = native Colyseus 0.16** (ตรวจ source ที่ติดตั้งจริง ก่อนตัดสิน — `@colyseus/core` 0.16.24): เปลี่ยน `server.define().filterBy(['mapId','channelId'])` → **`filterBy(['mapId','partyId'])`** + ตั้ง `room.maxClients` = capacity knob. ยืนยันจาก MatchMaker.js/Room.js: `findOneRoomAvailable` query room ที่ `locked:false` + ตรง filter → **room auto-lock เมื่อ `clients+reserved ≥ maxClients`** (Room.js) → เต็มแล้ว joinOrCreate **สร้าง room ใหม่** = auto-assign ตาม load/population เป๊ะ; filter fields ถูกเขียนลง room listing ตอน `handleCreateRoom` → driver match ได้จริง (ตรวจแล้ว). **Solo** (partyId="") share pool → packed by capacity, เต็ม→CH ใหม่. **Party** (partyId≠"") share filter → converge ลง room เดียว native (คนแรกสร้าง, ที่เหลือ join) — ไม่ต้อง custom matchmaking/seat-reservation. **channelId = server-assigned display label** (CH.n) จาก **`server/matchmaking/channel-registry.ts`** (pure module-level singleton: assign เลขว่างต่ำสุด onCreate / release onDispose reuse เลข) — ไม่ใช่ filter key แล้ว (ต่างจาก P0-08 stub ที่ client ส่ง channelId). **Party primitive (minimal)**: `partyId` ใน JoinOptions (client ตั้งผ่าน URL `?party=xyz`, pure `src/engine/net/party.ts`; invite/leader UI = P2 TODO); server เก็บ party↔channel binding **โดย delegate ให้ Colyseus filterBy** (ไม่ต้อง hand-rolled registry) → PlayerState.partyId + MapRoomState.partyId ให้ client รู้ party membership (สีต่าง P2). **Knob ใหม่** (`NetConfig`): `channelCapacity` (dev 8) + `partyChannelCapacity` (6 = ขนาด party สูงสุด GS §16, party ไม่ถูกแยก = "party สำคัญกว่า solo" §59.3); env `CHANNEL_CAPACITY`/`PARTY_CHANNEL_CAPACITY` override dev/test. Debug overlay เพิ่มบรรทัด `party:`. **DECISION ที่ตัดสินเอง (report ให้ owner): เลือก private-party-channel model** — party ได้ channel ของตัวเอง (native filterBy) แทน shared-population channel ที่ brief ร่างไว้ (ซึ่งต้อง custom matchmaking mutating matchmaker internals + capacity bump = เปราะกว่ามากใน never-downgrade realtime zone). Model นี้ **spec-supported** (TA §6 บรรทัด "Party/raid = private room") + การันตี party อยู่ด้วยกันเสมอ (แข็งกว่าที่ spec ขอ). **ผลข้าง: scattered-member prompt (§59.3) = N/A ใน P1** (สมาชิกไม่มีทางกระจัดเพราะ filter การันตี) — จด TODO ถ้า owner อยาก shared-population channel model ภายหลัง (จะต้องทำ custom matchmaking route + `client.consumeSeatReservation`). **TODO อื่น**: Redis channel allocation (scale หลาย node, TA §8 — ตอนนี้ single-process); no-channel-switch-mid-combat (P1 ยังไม่มี continuous combat state, จด). เทสต์ใหม่ pure: `tests/server-channel-registry.test.ts` (assign/release/reuse/per-map/format guard) + `tests/engine-net-party.test.ts` (resolvePartyId query parse). **Proof จริง (colyseus.js, server CHANNEL_CAPACITY=2, รันแล้วลบ ไม่ commit): ทุก assertion PASS** — **(1) solo auto-assign**: solo-1,2 → CH.1 (room เดียว), solo-3 → CH.2 (CH.1 เต็ม 2/2 → เปิด channel ใหม่); **(2) party sync**: partyA-1,2,3 → CH.3 (room เดียวกันหมด, **3 คนเกิน solo cap 2 ยังอยู่ด้วยกัน** = party cap 6 พิสูจน์ party priority), แยกจาก solo pool; **(3) คนละ party**: partyB-1,2 → CH.4 (≠ party A channel/room). server log ยืนยัน create (CH.n + cap ถูกต่อ solo/party) + dispose→release channel ครบ. **เกต: 514 tests (35 ไฟล์) + lint + build + `tsc -p server/tsconfig.json --noEmit` เขียวหมด**. ถัดไป: **P1-09 Click-to-move + pathfinding + touch** (TA §17.3 · L11).
- 2026-07-12: **P1-07 Reconnect 30s grace เสร็จ** (branch `feat/p1-world-sync`, GS §59.1 · TA §6). **Server hold state ผ่าน Colyseus `allowReconnection`** (API จริง 0.16: `allowReconnection(client, seconds): Deferred<Client>` เรียกใน `onLeave` → await; reconnect ทัน = resolve, หมด = reject/throw). `MapRoom.onLeave(client, consented?)` แยก 2 เส้น: **consented** (client เรียก `leave()` ตั้งใจ) → `removePlayer` ทันที · **หลุดไม่ตั้งใจ** (consented=false) → `await allowReconnection(client, graceSeconds)` — reconnect ทันใน grace = **ไม่ลบอะไร** → PlayerState/MoveTracker/cooldown ที่ผูก sessionId เดิม survive = ตำแหน่ง/channel/cooldown เดิม restore อัตโนมัติ (ไม่ผ่าน onJoin); grace หมด → `removePlayer`. `onJoin` ใช้ **`resolveSpawnPosition`** (pure §59.1) snap พิกัดที่ client ส่ง → safe camp ถ้าเดินไม่ได้/ไม่ finite (server = source of truth ว่าจุดลงได้จริง). **graceSeconds = knob** (`DEFAULT_ENGINE_CONFIG.reconnect.graceSeconds=30`; env `RECONNECT_GRACE_SECONDS` override dev/test). **Client** `net-client.ts`: เก็บ `reconnectionToken` หลัง join → `onLeave` code≠4000 (หลุด) = `beginReconnect` (exponential backoff `reconnectBackoffMs`/`shouldRetryReconnect` เข้า seat เดิม → สำเร็จ re-wire เงียบ ๆ + `resetTrackedEntities` กัน remote/mob ซ้ำจาก onAdd(immediate) รอบใหม่) · หมดสิทธิ์ = `freshJoin` (safe camp) · status ใหม่ **"reconnecting"** (debug overlay โชว์อัตโนมัติ). **Pure logic** `src/shared/reconnect.ts` (decideReconnect §59.1 decision table / resolveSpawnPosition / backoff) + `ReconnectConfig` knob (graceSeconds + clientRetry, mirror client/server) + optional **`safeCamp`** ใน MapConfig (fallback = spawnPoint ผ่าน `safeCampOf`, loader validate เหมือน spawnPoint; P0 Test Field ยังไม่ตั้ง = ใช้ spawnPoint). **พฤติกรรม remote ระหว่าง grace ที่เลือก**: Colyseus default = state ค้างจน expire → **client อื่นยังเห็น player ที่หลุดค้างอยู่** ตลอด grace (พิสูจน์แล้ว, ไม่เพิ่ม `connected` flag ใน schema เพื่อไม่ขยาย wire shape). **TODO anti-exploit death case** (จดใน MapRoom.onLeave): P1 ยังไม่มี player death/PvP → disconnect ไม่ได้ใช้หนีตาย; เมื่อมี combat death/PvP critical state (P2) ต้องเช็ค critical state ตอนหลุด → บังคับ safe camp (ชี้ §59.1 guardrail). เทสต์ใหม่ `tests/shared-reconnect.test.ts` + safeCamp cases ใน `tests/engine-map-loader.test.ts` (pure). **Proof จริง (colyseus.js in-process server, grace=2s ผ่าน env, รันแล้วลบ ไม่ commit): 9/9 PASS** — (1) reconnect ใน grace → sessionId/roomId เดิม + ตำแหน่ง X (tx=13) restore เป๊ะ · observer เห็น A ค้างทั้งก่อนหลุดและระหว่าง grace · (2) รอเกิน grace (2.6s) → `client.reconnect(token)` throw (seat หมดอายุ) → fresh join = safe camp (tx=12.5) + sessionId ใหม่ · (3) join พิกัด invalid (บ่อน้ำ 17.5,17.5) → snap safe camp (tx=12.5). **เกต: 500 tests (33 ไฟล์) + lint + build + `tsc -p server/tsconfig.json --noEmit` เขียวหมด**. ถัดไป: **P1-08 Channel auto-assign + party sync** (GS §59.3 · TA §6).
- 2026-07-12: **P1-06 Combat feel pass เสร็จ** (branch `feat/p1-world-sync`, TA §11 · GS §17.3/§17.5/§17.10) — ยกระดับ juice จาก stub (P0-10) เป็นของจริง. **1) Damage numbers**: `src/game/combat/damage-number.ts` refactor เป็น **BitmapText + object pool** (`src/engine/render/object-pool.ts`, generic pure — acquire/release/reuse, ไม่มี `new`/`.destroy()` ในเส้นทาง spawn/update ปกติ, poolSize default 400 knob 300–500); ติดตั้ง BitmapFont 3 style (normal/crit/aggregate — crit ใหญ่กว่า+สีต่าง GS §17.3); layer เป็น child เดียวของ `scene.world` ตรง ๆ (ไม่ผ่าน scene.addEntity ต่อเลข กัน DepthRegistry churn ทุก hit); เกิน quality-tier cap → `src/game/combat/damage-aggregate.ts` (pure window state) รวมยอดเป็นเลขก้อนทุก 0.5 วิ (GS §17.10). **2) Hit stop**: `src/game/combat/hit-stop.ts` (pure) — trigger เมื่อ crit/kill ระดับตาม `skill.hitStopLevel`; **time-scale เฉพาะ juice update** (damage number/hitbox debug fade) ใน `combat-stub.ts` เอง — cooldown/attack input/network/mob simulation ยังใช้ dt จริงเสมอ (ไม่แตะ). **3) Screen shake**: `src/engine/render/screen-shake.ts` (pure, engine layer เอง ไม่ import src/game) — decay real-time + `camera.ts` `applyShakeOffset` (บวกหลัง clamp เสมอ) + `scene.ts` `setCameraShakeOffset()`; trigger เมื่อ crit/kill **หรือ** `skill.screenShakeLevel ≥ alwaysTriggerAtLevel` (knob, ultimate-tier shake แม้ไม่ crit); `screenShake.enabled` = toggle ปิดได้ (GS §17.5). **4) Effect Quality**: `EffectQualityConfig` (Low/Med/High/Cinematic → maxConcurrentDamageNumbers/shakeAmplitudeScale/aggregateWindowScale ตัวเลขจริง) — P1 ใช้ default **medium** เสมอ (UI เลือก = P2 settings, ตามที่ brief อนุญาต). **5) Stress harness (F4, dev-only)**: `src/game/combat/stress-harness.ts` + `stress-harness-rate.ts` (pure accumulator) — กด F4 spawn มอนสังเคราะห์ ~40 ตัวผ่าน MobViewHandle ตรง ๆ (prefix `stress:`, bypass server/sim) + damage number ~300/วิ ผ่าน `combat.spawnSyntheticDamageNumber()` (**ใช้ pool/aggregate เดียวกับของจริง** ไม่ใช่ path แยก — พิสูจน์ budget ด้วยเส้นทางผลิตจริง). **Config**: `CombatFeelConfig` + `StressHarnessConfig` ใหม่ใน `engine/config.ts` (ตัด `DamageNumberConfig` เดิมออกจาก `CombatStubConfig` — ย้ายเข้า combatFeel.damageNumber ทั้งหมด). **zero-alloc hot loop (ตรวจเชิงโครงสร้าง ตาม brief — ไม่มี headless renderer วัด FPS อัตโนมัติ)**: object-pool factory เรียกเฉพาะจนถึง cap แล้วหมุนเวียนของเดิม (BitmapText ไม่ destroy/create ใหม่ต่อ hit), damage-number layer ไม่เรียก scene.addEntity/removeEntity ต่อเลข (เดิมทำทุก hit ใน P0-10), aggregate tick allocate array เล็กเฉพาะตอน flush จริง (≤2/วิ ไม่ใช่ hot path 300/วิ). เทสต์ใหม่ 6 ไฟล์ pure/deterministic (`engine-render-object-pool` 6 เคส, `engine-render-screen-shake` 10 เคส, `game-combat-damage-aggregate` 5 เคส, `game-combat-hit-stop` 7 เคส, `game-combat-stress-harness-rate` 6 เคส + เพิ่ม `engine-render-camera` applyShakeOffset 2 เคส) — **เกต: 484 tests (32 ไฟล์) + lint + build + `tsc -p server/tsconfig.json --noEmit` เขียวหมด**. **ไม่ทำ (จด scope)**: Effect Quality UI toggle จริง (P2 settings), loot juice (§17.12, ยังไม่มีระบบ loot), special damage text ("แตก!"/"BREAK" ฯลฯ §17.3 — ใช้ตัวเลขธรรมดา + prefix ×N สำหรับ aggregate เท่านั้น). **สำคัญสำหรับ review**: **owner กด F4 ที่ `/game` แล้วดู FPS จาก debug overlay (F3) เอง** — ไม่มี automated FPS assertion (ไม่มี headless WebGL renderer ใน test suite). ถัดไป: **P1-07 Reconnect 30s grace** (GS §59.1 · TA §6).
- 2026-07-12: **P1-05 Server combat authority เสร็จ** (branch `feat/p1-world-sync`, TA §15/§16.2/§16.3/§18.4/§6/§7 · never-downgrade zone: combat result calc — รีวิวไขว้). ย้าย combat จาก client dummy (P0-10) เป็น **server-authoritative เต็ม**. **Pure server-only** `src/game/combat/formula.ts` — สูตร §15.2 `DMG=ATK×baseMult×[k/(k+effDef)]`, `effDef=max(0,DEF−Pen)`, crit roll×(1+critDmg), ×bossModifier/pvpModifier/tierReduction; `computeSkillDamage` aggregate hitCount (crit แยกต่อ sub-hit); guard div-by-0/ไม่ติดลบ; RNG inject; **ห้าม client import** (comment header + known-traps). **Pure** `src/game/combat/cast-validation.ts` — `validateCast` (unknown_skill/cooldown/out_of_range) + `resolveSkillHits` (reuse findHits + maxTargets cap nearest-first §18.4) + skillAttackShape (§50.1→geometry). **Config knob (PENDING OWNER, จาก proposal)** `CombatBalanceConfig` (k=50, player นักดาบ lv1 [atk12/def8/critRate0.05/critDmg0.5/pen0], mob stat ต่อ type [slime hp45/def4, mushroom hp130/def10, tierReduction] — **mob HP ย้ายมาที่นี่ = single source of truth, ลบ combat.mobHp/defaultMobHp**) + MobConfig.hpBar. **Protocol**: `MSG_CAST_SKILL`{skillId,aim,dir} → `MSG_SKILL_RESULT`{casterId,skillId,hits:[{mobId,dmg,crit,killed}]} broadcast · `MSG_CAST_REJECTED` เงียบ ๆ · **ลบ MSG_DEBUG_KILL_MOB**. **Server** `MapRoom.handleCast`: load skills (loadSkillDefinitions WARRIOR_SKILLS) + cooldown per-player per-skill (server clock) → validate → resolveSkillHits จาก sim → computeSkillDamage → `sim.damageMob` (ตาย→despawn+respawn ผ่าน API P1-03) → sync + broadcast. **Client**: combat-stub ส่ง cast intent + เล่น anticipation animation ทันที (ไม่รอ server §6) + onSkillResult → damage number จากผล server จริง (P0-10 layer เดิม; feel เต็ม = P1-06); offline = dummy playground ไม่ฆ่า (non-authoritative). **HP sync**: mob hp update จริง + HP bar เล็ก ๆ เหนือมอนที่ hp<maxHp (mob view manager wrap animator+bar ใน container). เทสต์ใหม่ 2 ไฟล์ (`game-combat-formula` 26 เคส: DEF=k→ครึ่ง/DEF=0→เต็ม/Penetration/crit/modifier/ไม่ติดลบ/guard div-0/hand-calc §1.4 slime24·mushroom22/hitCount aggregate; `server-cast-validation`: cooldown/range/shape/maxTargets cap/validate ลำดับ). **เกต: 448 tests (27 ไฟล์) + lint + build + `tsc -p server/tsconfig.json --noEmit` เขียวหมด**. **Proof จริง (colyseus.js, รันแล้วลบ ไม่ commit): 7/7 PASS** — server spawn 7 มอน · เดินเข้าใกล้ slime → cast royal_wave → skill_result dmg=24 (ตรง 12×2.2×50/54 เป๊ะ, mushroom แยกรอบ=22 ตรง DEF10) · ตี 2 hit ฆ่า (killed=true, cast#4 โดน 2 ตัว = AoE cone) · มอบ despawn จาก state · respawn ใน 5s (alive 6→7) · cast รัว→ปฏิเสธ cooldown · skillId ปลอม→ปฏิเสธ unknown_skill. **ไม่ทำ (จด scope)**: loot/EXP/progression (P2, ผู้เล่นทุกคน lv1 นักดาบ) · status effects/CC (เก็บ definition ยังไม่ apply) · resource/mana pool (proposal §5[8] PENDING OWNER) · AoE spatial hash (§11) = linear scan พอที่ 30 CCU + TODO · combat feel เต็ม (hit stop/shake/BitmapText pool) = P1-06. **Cross-review fix (BLOCKER)**: `warrior-skills.ts` เดิม (full SkillDefinition) ถูก client (app.ts) static import → balance literal (baseMultiplier/bossModifier/maxTargets ฯลฯ) รั่ว browser bundle แม้ clientView() ตัดตอน runtime (bundler ไม่ตัด property ใน object literal) → **แยก 2 ไฟล์**: `src/game/skill/data/warrior-skills-server.ts` (full, SERVER-ONLY) + `warrior-skills-client.ts` (ClientSkillView 28 field, client import ตัวนี้); ลบ warrior-skills.ts เดิม; drift guard test `client == clientView(server)`; grep ยืนยัน client bundle ไม่มี server-only literal (app.ts/combat-stub import แค่ client manifest + hit-test geometry). known-traps เพิ่ม trap "import ข้อมูล server-only = รั่วเท่ากับ import สูตร". RECOMMEND: TODO(P2 skill ownership/class/unlockLevel) + TODO(ground-target aimPos) ใน MapRoom + proposal checklist [12] จุด round multi-hit. **เกต: 448 tests (27 ไฟล์) + lint + build + server tsc เขียว**. ถัดไป: **P1-06 Combat feel pass** (TA §11 · GS §17).
- 2026-07-12: **P1-04 Skill schema loader เสร็จ** (branch `feat/p1-world-sync`, GS §50.1 · TA §16.1) — ไฟล์แรกใน `src/game/skill/**`, ยังไม่มีการ cast จริง (P1-05 scope). `src/game/skill/types.ts` — `SkillDefinition` ครบ **37 field ตรงชื่อ/ลำดับ §50.1 เป๊ะ** (copy ตรงจาก checkpoint, ไม่พิมพ์จากความจำ) + `SKILL_FIELD_NAMES` (readonly tuple derive จาก `keyof SkillDefinition` กันหลุด sync) + comment บันทึกการตีความ type ต่อ field ที่คลุมเครือ (union เปิดไว้เป็น `string` แทน enum ปิด เพราะ §50.1 ไม่ได้ freeze ค่า และอีก 4 อาชีพยังไม่เคาะเต็ม; `branch`/`damageType`/`scalingStat`/`radius`/`angle`/`statusEffects`/`crowdControl` = nullable ตามที่ proposal ใช้ null จริง). `src/game/skill/loader.ts` — `loadSkillDefinitions(raw: unknown[])` (pure, ไม่ใช้ zod match pattern `engine/map/loader.ts`) fail-loud: field ขาด/type ผิด/ค่าติดลบที่ไม่ควร (cooldown/range/baseMultiplier ฯลฯ ≥0, maxTargets/unlockLevel ≥1)/**unknown field = error** (กัน typo เงียบ, เช็คกับ `SKILL_FIELD_NAMES`)/skillId ซ้ำ → `SkillDefinitionError` ระบุ path field ชัด. `src/game/skill/views.ts` — แบ่ง field ตาม TA §16.1: `serverView` (copy ครบ 37) · `clientView`+`ClientSkillView` ตัด **9 server-only field** (baseMultiplier/scalingStat/damageType/maxTargets/hitCount/bossModifier/pvpModifier/crowdControl/serverAuthority) ตรงตามที่ §16.1 ระบุเป๊ะ. **Gap ที่เจอ (บันทึกไว้ในโค้ด ไม่ตัดสินเอง)**: §16.1 จัดหมวด field ไว้แค่ 34/37 ตัว — `skillName`/`description`/`statusEffects` ไม่ถูกจัดกลุ่มชัดเจน (server-only/shared/client-only/bot/meta); เลือกเก็บไว้ใน clientView (UI ต้องโชว์ชื่อ/description, statusEffects เป็น tag string ไม่มี magnitude เสี่ยงรั่ว balance ต่ำ) — ถามค้างถ้า tech/design อยากล็อกหมวดชัดเจนกว่านี้ต้องอัปเดต §16.1. `src/game/skill/warrior-skills.ts` — นักดาบ 4 skills (S1 basic/S2 AoE farming/S3 boss single-target/S4 utility taunt) copy ค่าตรงจาก `docs/design/proposals/deungpu_P1_BALANCE_PROPOSAL_v1.md` §3.1 ทุก field — header **PENDING OWNER**. เทสต์ใหม่ `tests/game-skill-loader.test.ts` (71 เคส): 37 field ครบ/ลำดับตรง §50.1 + ทุก field (37 ตัว) ขาดทีละตัว→throw ระบุชื่อ + type ผิด (string/boolean/array)→throw + ค่าติดลบที่ไม่ควร→throw + unknown field (รวม typo `baseMultiplyer`)→throw ระบุชื่อ + skillId ซ้ำ→throw + WARRIOR_SKILLS ทั้ง 4 ผ่าน validation จริงครบ 37 field + serverView ครบ/copy ใหม่ + clientView ตัด server-only ครบทุก skill. เกต: **408 tests** (25 ไฟล์) + lint + build + `tsc -p server/tsconfig.json --noEmit` เขียวหมด. ถัดไป: **P1-05 Server combat authority** (§15, §6, §7 — cast_skill intent → validate → damage formula จริง).
- 2026-07-12: **P1-03 Server-side mob simulation เสร็จ** (branch `feat/p1-world-sync`, TA §18/§6/§11) — ย้าย mob จาก client-local (P0-09) เป็น **server-authoritative**. **Pure core (server+offline ใช้ร่วม, ไม่มี pixi):** `src/game/mob/ai.ts` (aggro/leash/pull cap §18.3 + AI LOD §6/§11 + respawn timer — decision fn เดี่ยว ๆ เทสต์ได้) · `src/game/mob/simulation.ts` (`createMobSimulation` authoritative stateful: spawn ชุดแรก [spawnAllPockets เดิม] + respawn ทีละตัวเมื่อ `killMob` [respawnDelayMs, clock inject, ≤ activeCap] + AI state machine wander↔chase↔return [reuse stepWander/stepMovement] + LOD active/asleep). **AI state machine:** wander (aggro เช็คก่อนเดิน → chase ถ้ามี player ในรัศมี+cap ไม่เต็ม) → chase (stepToward player; leash เมื่อ target หาย/ลากเกิน leashRadius/หนีเกิน deaggroRadius → return) → return (stepToward spawnOrigin; ถึง → wander). pull cap ต่อผู้เล่นสร้างใหม่ทุก tick + จองสิทธิ์ทันทีกัน over-pull. **Server glue:** `MapRoom` สร้าง sim + `setSimulationInterval(ai.tickHz 10Hz)` → ป้อน player pos → `sim.tick` → เขียน `state.mobs` (MapSchema `MobState{mobId,mobType,tx,ty,state,hp}`) + `MSG_DEBUG_KILL_MOB` handler (ทดสอบ death→respawn ก่อน P1-05). **Client:** refactor `mob/manager.ts` → **view manager** (render/interpolate ผ่าน buffer P1-01 เหมือน remote player, facing derive จาก delta ไม่ sync ทิศ) + `app.ts` **mob source controller** (online=server callbacks / offline=รัน sim ตัวเดียวกัน local ป้อน view — graceful fallback, สลับตาม net status) + combat stub retarget มอน server, **เล่นแค่ effect ไม่ฆ่า** (TODO P1-05). **knob ใหม่ (config, PENDING OWNER tune):** MobAiConfig (tickHz 10/chaseSpeed 2.4/aggroRadius per type/leashRadius 8/deaggroRadius 9/pullCap 10 [§18.3 8–12]) + MobLodConfig (aoiRadius 14/idleTickHz 2) + respawnDelayMs 5000 (override ต่อ pocket ได้). **AOI filter §18.2:** ยังไม่บังคับ (30 CCU/map เล็ก) — จุด filter ชัดเจน + TODO ใน `MapRoom.syncMobsToState`. **Proof จริง (colyseus.js, รันแล้วลบ ไม่ commit):** 8/8 PASS — server spawn 12 มอน · เดินเข้าใกล้ → มอน aggro ไล่ (dist 1.70→0.02) · หนีลงใต้ (คอลัมน์ tx=13 เดินได้) → มอนถูกลากออก pocket (ty→11.23) → leash → return กลับเข้า pocket area (15.42,5.68) · debug kill → หายจาก state → respawn id ใหม่ใน 5s. (หมายเหตุ: รอบแรก flee ทับ blocked tile → server P1-02 validation ตี move กลับจริง = ยืนยัน collision ทำงาน). เกต: **337 tests** (2 ไฟล์ใหม่: game-mob-ai, game-mob-simulation) + lint + build + `tsc -p server/tsconfig.json` เขียว. ถัดไป: **P1-04 skill schema loader (§50.1)**.
- 2026-07-12: **P1-02 Server-authoritative movement เสร็จ** (branch `feat/p1-world-sync`) — server เลิก trust ตำแหน่ง client, validate จริง (TA §6/§7/§16.3). ใหม่: `src/shared/movement-validation.ts` (pure validateMove: non_finite→teleport→speed→blocked, clamp elapsed กัน clock skew) + `MSG_POSITION_CORRECTION` (net-protocol) + `MovementValidationConfig` (config knob, mirror client/server). `server/rooms/MapRoom.ts` onCreate โหลด map (loader เดิม) + reuse engine collision (snapToTile+isWalkableTile) + อ่าน knob จาก `DEFAULT_ENGINE_CONFIG` (**single source of truth — ไม่ copy สูตร**) → validate ทุก MSG_MOVE, ผิด→ไม่ apply + ส่ง correction กลับ (per-player MoveTracker + cooldown). Client: `net-client` รับ correction (นับ correctionCount → debug overlay) → `app.ts` hook → `local-player.applyCorrection()` snap-only reconcile. **Share strategy**: server import engine pure modules ผ่าน `@/` alias — tsx resolve paths จาก `server/tsconfig.json` ได้ (พิสูจน์แล้ว, ไม่ลาก pixi เข้า bundle). Proof (scratchpad, ไม่ commit): client ปกติเดินไม่โดน correction · client โกงส่ง teleport → โดน snap กลับตำแหน่ง valid. เกต: 304 tests + lint + build + `tsc -p server/tsconfig.json` เขียว.
- 2026-07-12: **P1-01 Netcode interpolation เสร็จ** (branch `feat/p1-world-sync`) — remote player ยกจาก lerp ง่าย (P0-07) เป็น **snapshot interpolation buffer** (render ย้อนหลัง bufferMs=120): `src/engine/net/interpolation.ts` (pure ring buffer + sampleAt, out-of-order=drop, starved=extrapolate สั้น+clamp maxExtrapolationMs=100, เกิดใหม่=clamp ไม่ลากจากไกล) + refactor `remote-player-manager.ts` (push+sample, inject clock) + `NetInterpolationConfig` (config knob) + `tests/engine-net-interpolation.test.ts` (15 เทสต์). local ยัง full client-predict — reconcile hook วาง TODO ไว้รอ **P1-02**. เกต: 287 tests + lint + build เขียว.

## Older work

- 2026-07-12: **P1 Balance Proposal v1** (deep-worker, docs-only ไม่แตะโค้ด) — `docs/design/proposals/deungpu_P1_BALANCE_PROPOSAL_v1.md` สถานะ PROPOSAL/PENDING OWNER (ไม่ใช่ spec จนกว่าเคาะ, เข้า spec ผ่าน §59.4). เสนอ: ค่า **k=50** (range 30–80, anchor: DEF=k → มิติเกชัน 50%) + ตาราง damage ตัวอย่าง; **stat baseline นักดาบ lv1–10** (10 stats §15.1) + secondary; **มอน Map 1** ดึ๋งปุ๊/หมูพอง/elite/boss (HP·ATK·DEF·tierReduction §15.5); **ตาราง skill 5 อาชีพ** ตาม field §50.1 เป๊ะ (นักดาบ 4 skill full YAML, อีก 4 อาชีพ 3 skill draft table — ใช้ชื่อ skill จาก §17.6 map เข้า 3 branch §8); guardrail check §48.9 (farming แพ้ boss single-target ~3.8×, AoE ไม่ one-shot elite ต่ำสุด 7 hit, maxTargets 6 ตาม §56.4). **จุดต้องเคาะ (checklist ท้ายไฟล์)**: k, stat, mob, ตาราง skill, + 2 spec-gap — resourceCost มีใน §50.1 แต่ 10-stat ไม่มี resource pool (P1 รัน cooldown-only ได้), damageType physical/magic ยังไม่ split resist
- 2026-07-12: **P0-12 P0 Handoff Check** (qa-specialist audit, ห้ามแก้ production code) — ตรวจ P0 Done Definition ครบ 12/12 ข้อ PASS ด้วยหลักฐานจริง: gate ทั้งหมดเขียว (`npm test` 272/272 ผ่าน 20 ไฟล์, lint สะอาด, `next build` สำเร็จ, `tsc -p server/tsconfig.json --noEmit` สะอาด); live check GET `/game` → 200; เขียน proof script ชั่วคราว (colyseus.js, วางใน repo root ชั่วคราวตาม known-traps tsx-resolve-trap, รันแล้วลบทันที ไม่ commit) พิสูจน์ข้อ 7/8 จริง: 2 client join mapId+channelId เดียวกัน → roomId เท่ากัน, state มี mapId/channelId/roomId ถูกต้อง, players.size=2 หลัง join ทั้งคู่, move sync ข้าม client เห็นจริง, leave → players.size ลด (entity หาย); client ที่ 3 ใช้ channelId="CH.2" → roomId ต่างจาก CH.1 สนิท, state แยกกันจริง (isolate). ข้อ 2/3/4/5/9/10 (visual) ยืนยันจาก unit tests ที่มีอยู่แล้ว (engine-iso-*, engine-render-*, engine-movement-direction, engine-animation-manifest, game-mob-spawn/wander, game-combat-hit-test) — mark "verified by tests, visual จริง = owner review ตอน PR" (ไม่มี jsdom/WebGL render test ใน P0). ข้อ 12 (scope): grep `src/`+`server/` หา login/inventory/market/guild/PvP/monetization ฯลฯ — เจอเฉพาะ TODO comment ชี้ P1 ทั้งหมด ไม่มี implementation จริง; `package.json` dependencies ตรวจแล้วสมเหตุผลกับ P0 scope. Docs consistency: CODEMAP เทียบกับ `find src server -type f` ตรงกันครบ (ไม่มีไฟล์ตกหล่น/เกิน), `tests/docs-guard.test.ts` ผ่านอยู่แล้วใน gate. **ผลสรุป: P0 complete, pending owner visual review + PR** — ไม่พบ bug/ความไม่ตรง spec ระหว่างตรวจ, ไม่ต้องแก้ production code
- 2026-07-12: **P0-11 Debug Overlay** — P0_SCOPE_LOCK §4.10 (fps · player tile/world coord · mapId · roomId · channelId+status · entity count · pointer tile · depth-sort debug toggle). **Pure**: `src/engine/runtime/debug-info.ts` (`EngineDebugInfo` shape + `buildDebugInfo()` ปัด fps เป็นจำนวนเต็ม + playerTile/pointerTile 2 ตำแหน่งทศนิยม ที่เดียว + `IDLE_NET_DEBUG_INFO` fallback เมื่อ `net.enabled=false`) · `src/ui/debug-overlay-logic.ts` (`DebugOverlayState{visible,depthDebug}` + `isDebugToggleKey`("F3")/`toggleVisible`/`toggleDepthDebug` reducer, แยกจาก component ให้เทสต์ได้). **Engine glue**: `src/engine/runtime/app.ts` เพิ่ม pointer tile tracking (`pointermove`/`pointerleave` บน `app.canvas`, แปลง client coord → canvas-relative (`getBoundingClientRect`) → ลบ `scene.world.position` → `screenToTile`+`snapToTile` — canvas CSS px กับ `scene.world`/`viewport` อยู่ space เดียวกันเพราะ `autoDensity=true`) + `EngineHandle.getDebugInfo()`/`setDepthDebug()` (passthrough ไป scene). `src/engine/render/scene.ts` เพิ่ม `entityCount` getter (จาก `DepthRegistry.size`) + `setDepthDebug(enabled)`: สร้าง `depthDebugLayer` (sibling ของ `entityLayer` ใน `world`, วาดทับบนสุด, ไม่ใช้ zIndex ร่วมกับระบบ depth sort จริงกัน conflict) — เปิด = สร้าง/sync `Text` แสดง depth rank เหนือทุก entity ทุกครั้งที่ `syncDepth()` resort (dirty-gated เหมือนเดิม), ปิด = ลบ label ทั้งหมดทันที (ไม่มี label ค้าง ไม่กระทบ perf ตอนปิด). `src/engine/config.ts` เพิ่ม `DebugOverlayConfig` (defaultVisible/pollIntervalMs=250/depth label color·fontSize·offsetY, shallow-merge ใน `createEngineConfig`). **UI**: `src/ui/DebugOverlay.tsx` ("use client") — panel มุมจอ poll `getDebugInfo()` ทุก `pollIntervalMs` ผ่าน `setInterval` (**ไม่ subscribe ทุก frame**, ตาม tech §2), รับ `getHandle: () => EngineHandle | null` (accessor function ไม่ใช่ handle ตรง ๆ) ทนกรณี engine ยัง init ไม่เสร็จ/ถูก destroy; แสดงครบทุกค่าตาม scope + ปุ่ม toggle depth debug (เรียก `setDepthDebug`) + ปุ่ม/คีย์ลัด F3 ซ่อน-แสดง panel (`preventDefault` กัน browser). `src/ui/GameCanvas.tsx` เก็บ `EngineHandle` ใน `useRef` (**ไม่ใช่** `useState` — กัน world-state-เข้า-React-state) + render `<DebugOverlay>`. FPS text เดิมในแคนวาส (per-frame, ticker) **คงไว้ตามเดิมไม่ย้าย** — overlay แสดงค่า poll เฉลี่ยแยกต่างหาก พอสำหรับ debug (ตัดสินใจนี้ document ไว้ตรงนี้ตามที่ brief อนุญาตให้เลือกเอง). เทสต์ใหม่: `tests/engine-runtime-debug-info.test.ts` (roundTile + buildDebugInfo shape/rounding + IDLE_NET_DEBUG_INFO), `tests/ui-debug-overlay-logic.test.ts` (isDebugToggleKey + toggle reducer pure/ไม่ mutate), เพิ่มเคสใน `tests/engine-config.test.ts` (DebugOverlayConfig default+override) — รวม 272 เทสต์เขียว, lint เขียว, build เขียว. **ไม่ทำ**: Zustand bridge จริง (P1 — จด TODO ในโค้ด/docs), React component render test (ไม่มี jsdom WebGL — ตาม brief), depth-sort visual QA ด้วยตา (ปล่อยให้ owner/QA ลองที่ `/game` กด toggle เอง)
- 2026-07-12: **P0-10 Combat Stub** — chain input→animation→hit→feedback (dummy เท่านั้น, P0_SCOPE_LOCK §4.9). **Pure logic**: `src/game/combat/hit-test.ts` — `findHits(attackerPos, facing, targets, tileSize, shape)`: ระยะ = euclidean บน **tile coords** ตรง ๆ, arc = มุมรวม (องศา) รอบทิศ facing แต่เทียบเป็น **มุมบนจอ** (project ทั้ง facing ผ่าน `directionToScreenUnit` และ target-vector ผ่าน `tileToScreen` ของ delta แล้ว `atan2(-sy,sx)` สูตรเดียวกับ `resolveDirection`) เพราะ diamond 64×32 ไม่ใช่สี่เหลี่ยมจัตุรัส มุม tile ดิบจะเบี้ยวไม่ตรงกับที่ตาเห็น; ระยะ 0 เป๊ะถือว่าโดนเสมอ (มุมไม่มีความหมาย) — เหตุผลเต็มอยู่ header comment ไฟล์. เพิ่ม `rollDummyDamage`/`advanceCooldown`/`canAttack`/`applyDummyDamage` (hp/death transition, pure — mob/manager.ts เป็นคน apply ผลลง instance จริง) + helper `screenAngleForDirection`/`tileUnitVectorForScreenAngle` (reuse วาด hitbox debug wedge ให้ตรงเกณฑ์ hit จริงเป๊ะ ไม่ใช่วงกลมเดา). **Pixi glue**: `src/game/combat/damage-number.ts` (`createDamageNumberLayer` — Text ลอยขึ้น+fade ผ่าน scene entity API เดิม [addEntity/removeEntity, zLayer 50 สูงสุด], สร้าง/ทำลายตรง ๆ ต่อ hit — TODO(P1) BitmapText+pool ตาม tech §11) · `src/game/combat/combat-stub.ts` (`createCombatStub` — cooldown gate (config) → `player.triggerAttack()` → `findHits` กับ `mobs.getAliveTargets()` → `mobs.applyDamage()` (dummy roll) + `damageNumbers.spawn()`, hitbox debug = wedge polygon sample จาก `tileUnitVectorForScreenAngle` ตาม arc/radius จริง fade ตาม config, zLayer 40). **Engine เปลี่ยน**: `src/engine/input/keyboard.ts` เพิ่ม `ATTACK_KEY` ("Space") edge-triggered `consumeAttackPressed()` (กันกดค้างสแปม, ไม่กระทบ MOVE_KEYS เดิม) · `src/engine/player/local-player.ts` เพิ่ม `triggerAttack()`/`isAttacking`/`consumeAttackPressed()` (ล็อก animation="attack" จนจบคลิปตาม `attackFrameDuration×attackFrames` แล้วกลับ idle/walk เอง, เดินระหว่างโจมตีได้ตำแหน่งขยับปกติ) · `src/game/mob/manager.ts` เพิ่ม hp ต่อ mobType (จาก `combat.mobHp`), `applyDamage()`/`getAliveTargets()`, death feedback (squash แนวตั้ง + fade บน `animator.view` ตรง ๆ ไม่ชน mirror scale.x) แล้ว despawn จริงหลัง `deathFeedback.durationMs` (ไม่ respawn ใน P0) · `src/engine/config.ts` เพิ่ม `CombatStubConfig` (attack shape radius/arcDegrees/cooldownMs, dummyDamage range, mobHp per type + defaultMobHp, hitboxDebug enable/color/alpha/durationMs, damageNumber style/timing, deathFeedback — ทุกค่า Design Knob ห้าม hardcode) · wire เข้า `src/engine/runtime/app.ts` (update หลัง mobs.update, destroy ก่อน mobs.destroy). เทสต์ใหม่ `tests/game-combat-hit-test.test.ts` (259 เขียวรวม): 8 ทิศ hit/miss (halfArc >45° กันชน float-tie) + boundary near-tie แยกเทสต์ (44°/46° รอบ facing, pattern เดียวกับ `engine-movement-direction.test.ts`) + หลังผู้เล่นไม่โดน + ระยะ 0 โดนเสมอ + ระยะเกิน radius พลาดแม้ตรงหน้าเป๊ะ + rollDummyDamage ในช่วง (หลาย seed, deterministic, min=max ไม่เรียก rng) + cooldown gate เต็มรอบ + hp/death transition (รวม damage เกิน hp → ติดลบได้ไม่ clamp). lint/build เขียว. **ไม่ทำ** (P1, ตาม P0 §4.9): full damage formula (multiplicative diminishing, tech §15.2), skill schema จริง (GS §50.1), item drop/EXP/gold, boss mechanic, mob respawn, damage number pooling เต็มระบบ (TODO ชี้ tech §11 ในโค้ด)
- 2026-07-12: **P0-09 Dummy Mob Pocket Spawn** — ไฟล์แรกใน `src/game/**` (game logic บน engine, ใช้ engine ผ่าน public API เท่านั้น). **Pure logic**: `src/game/mob/rng.ts` (RngFn inject ได้ — `defaultRng`=Math.random runtime, `createLcgRng` seeded deterministic ในเทสต์) · `src/game/mob/spawn.ts` (`spawnPocketMobs`/`spawnAllPockets` — TA §18.1 fixed pocket + random point inside: จำนวน = random(packSize.min..max) clamp ด้วย activeCap, จุดเกิด = `findWalkableSpawnPoint` สุ่มใหม่ถ้าโดน blocked จำกัด `maxPlacementAttempts` รอบ แล้ว best-effort ข้ามถ้าหาไม่เจอ — ไม่ throw) · `src/game/mob/wander.ts` (`createWanderState`/`stepWander` — สลับ idle/walk สุ่มช่วงเวลาจาก `MobWanderConfig`, เดินจริงใช้ `stepMovement` เดิม [reuse ฟรี, ไม่เขียน collision ใหม่] + **leash แบบง่าย**: ผูก pocket.area เป็นเงื่อนไข block เพิ่มใน predicate ที่ส่งเข้า stepMovement → มอนไถลติดขอบเหมือนชนกำแพง ไม่หลุด area). **Pixi glue**: `src/game/mob/manifest.ts` (reuse `AnimationManifest`/`resolveClip` ของ engine — เลือกวาดจริงแค่ 2 ทิศ [S,N] + mirror 6 ทิศ เพราะ mob dummy symmetric ไม่มี asymmetry ซ้าย-ขวาแบบ player, ทิศยังคำนวณ+ส่งเข้า animation จริงเผื่อ P1 ใส่ art ไม่สมมาตร) · `src/game/mob/placeholder.ts` (`generateMobTextures` — Graphics→RenderTexture ต่อ mobType: slime=ก้อนเขียวเด้ง squash-stretch, mushroom=หมวกแดง+ก้าน; generate **ครั้งเดียวต่อ mobType** แชร์ข้าม instance กัน GPU เปลือง) · `src/game/mob/manager.ts` (`createMobManager` — spawn ทุก pocket ตอนสร้าง, ticker update wander+animator+moveEntity, destroy แยก texture ออกจาก instance เพราะแชร์กัน [ห้ามเรียก `animator.destroy()` ต่อตัว — จะ destroy texture ของตัวอื่นที่ยังใช้อยู่]). เพิ่ม `MobConfig` (spawn/wander/animation/defaultStyle/styles) ใน `src/engine/config.ts` (Design Knob ทั้งหมด, ไม่ hardcode) + wire เข้า `src/engine/runtime/app.ts` (spawn ตอน createEngine, update ใน ticker, destroy ตอนปิด engine). เทสต์ pure ใหม่ 3 ไฟล์ (`tests/game-mob-{rng,spawn,wander}.test.ts`, 245 เขียวรวม): RNG determinism, จำนวน spawn ในช่วง packSize/clamp activeCap, จุดเกิดอยู่ใน pocket.area และเดินได้จริง (หลาย seed + P0_TEST_FIELD จริง), wander interval/speed มาจาก config (ไม่ hardcode), leash ไม่หลุด area (deterministic + seeded random หลายร้อย step, ทั้งกรณี block ด้วย area อย่างเดียวและ block ด้วย map จริงร่วมด้วย). lint/build เขียว. **TODO จดในโค้ด (P1)**: server-authoritative spawn/AI (TA §18.1/§7), object pooling (tech §11 — P0 มอนไม่ตายเพราะยังไม่มี combat จึงยังไม่ pool, แต่โครง `Map<id, MobInstance>` ไม่ขวางเพิ่มทีหลัง). **ไม่ทำ** (ตาม scope P0 §4.8): loot/EXP/aggro/combat (P0-10), respawn, mob AI จริง
- 2026-07-12: **P0-08 Channel Stub** — channelId เป็น first-class field: `src/shared/net-protocol.ts` `JoinOptions.channelId` (client ส่งจริง, default constant `DEFAULT_CHANNEL_ID` ไม่ hardcode ซ้ำฝั่ง server) + `src/engine/config.ts` `NetConfig.channelId` (Design Knob ใหม่, default `DEFAULT_CHANNEL_ID`) + `runtime/app.ts` ส่ง channelId ใน joinOptions จริง. **Server**: `server/index.ts` ผูก `gameServer.define(MAP_ROOM_NAME, MapRoom).filterBy(['mapId','channelId'])` (Colyseus 0.16 `RegisteredHandler.filterBy` — ดึงค่าจาก client joinOptions ตรง ๆ ตอน matchmake/create, ไม่ต้องพึ่ง metadata แยก); `MapRoom.onCreate` อ่าน `options.channelId ?? DEFAULT_CHANNEL_ID`; roomId จริง (`this.roomId`) เข้า state — มีอยู่แล้วตั้งแต่ P0-07 (ยืนยันซ้ำ). **Client debug API**: `net-client.ts` เพิ่ม `getNetDebugInfo(): {status, mapId, roomId, channelId, playerCount}` (playerCount คำนวณผ่าน pure helper ใหม่ `computePlayerCount` ใน `sync.ts`, online = remoteCount+1 มิฉะนั้น 0) ให้ P0-11 debug overlay ใช้แทนอ่าน `status` ดิบ. **Manual proof (ชั่วคราว, รันแล้วลบ ไม่ commit)**: start `dev:server` จริง → colyseus.js client A1 join channel "CH.1", A2 join channel "CH.1" (map เดียวกัน), B1 join channel "CH.2" (map เดียวกัน) → ผล: A1.roomId === A2.roomId (same-channel = same room, PASS), A1.roomId !== B1.roomId (diff-channel = diff room, PASS), `state.roomId` ตรงกับ `room.roomId` จริงของ Colyseus ทั้ง 3 client (PASS) — proof PASSED ทั้งหมด. เทสต์ pure ใหม่: `tests/engine-net-sync.test.ts` (JoinOptions.channelId shape + computePlayerCount 6 เคส) + `tests/engine-config.test.ts` (NetConfig.channelId default + override). รวม 222 เทสต์เขียว (14 ไฟล์), `npm run lint` เขียว, `npm run build` เขียว, `tsc -p server/tsconfig.json --noEmit` เขียว. **ไม่ทำ** (P1, tech §6/RUNTIME §4): auto-assign channel จริง, UI เลือก channel, party sync, channel capacity
- 2026-07-12: **P0-07 Realtime Room Skeleton** — Colyseus (matched 0.16 line: `colyseus@0.16.5` + `@colyseus/schema@3` server · `colyseus.js@0.16.22` client, schema 3 ทั้งคู่ = wire-compatible) + `tsx` dev runner. **Server** (`server/`, แยก process ตาม L4, own tsconfig legacy-decorators): `index.ts` (Server+define+listen), `rooms/MapRoom.ts` (onCreate/onJoin/onMessage move/onLeave — P0 trust position), `schema/MapRoomState.ts` (PlayerState{tx,ty,direction,anim} + MapRoomState{mapId,channelId,roomId,players}). **Shared** `src/shared/net-protocol.ts` (wire contract, ไม่มี runtime dep). **Client** `src/engine/net/{sync,net-client,remote-player-manager}.ts` (pure throttle/coerce + colyseus.js glue graceful-offline + remote entity lerp สีต่าง) + `config.ts` NetConfig + `runtime/app.ts` wire net เข้า ticker (EngineHandle.net สำหรับ P0-11) + `local-player.ts` expose animation + `GameCanvas.tsx` env url. **พิสูจน์จริง**: start server + 2 colyseus.js client → A/B อยู่ room เดียวกัน, B เห็น A spawn (3,3), A ขยับ→B เห็น (7.5,4.25) dir=E anim=walk, A leave→B ได้ onRemove; channelId=CH.1. เทสต์ pure `tests/engine-net-sync.test.ts` (216 เขียวรวม), lint/build เขียว (server แยกออกจาก Next build/lint). **ยังไม่ทำ (P1, จด TODO ในโค้ด)**: reconnect 30s grace (tech §6/§59.1), server-authoritative movement validation (tech §6), channel auto-assign+party sync (RUNTIME §4), persistence, deploy Render
- 2026-07-12: **P0-06 Sprite Animation Prototype** — data-driven animation (tech §17.4, L15): `src/engine/animation/manifest.ts` (pure `resolveClip`: logical 8-dir → sprite source + mirror flag · 5 ทิศวาด S/SW/W/NW/N + mirror SE←SW/E←W/NE←NW · รองรับ 8-dir override = ประกาศครบ 8 ไม่ mirror · `advancePlayhead` pure frame timing/loop · `createPlayerAnimationManifest`) + `src/engine/animation/player-placeholder.ts` (generate placeholder texture ด้วยโค้ด Graphics→RenderTexture — ตัวละคร asymmetric [accent แดงข้างเดียว] ให้เห็น mirror ด้วยตา, walk/idle/attack, foot anchor คงที่) + `src/engine/animation/animator.ts` (เล่นเฟรมบน Sprite, mirror = scale.x=−1 รอบ anchor.x=0.5 → เท้าไม่ย้าย) + `config.ts` `PlayerAnimationConfig`/`PlayerSpriteStyle` + `local-player.ts` ใช้ animator (walk/idle ตาม intent, ถอด nose ออก). เทสต์ `tests/engine-animation-manifest.test.ts` (37 เคส: 8 ทิศ×idle/walk + 8-dir override + error + timing/loop). รวม 204 เขียว, lint/build ผ่าน. **ยังไม่ทำ**: art จริง (ใช้ placeholder), mob animation wire (P0-09), attack input (P0-08), server sync (P0-07)
- 2026-07-12: **P0-05 Local Player Movement Prototype** — `src/engine/input/keyboard.ts` (intent tracker: WASD+arrows → tile-space intent ผ่าน inverse projection · pure `intentFromKeys`) + `src/engine/movement/{mover,direction}.ts` (pure: `stepMovement` เดินต่อเนื่อง+normalize เฉียง+collision slide แยกแกน+clamp dt · `resolveDirection` tile→8-dir จากมุมบนจอ + `directionToScreenUnit`) + `src/engine/player/local-player.ts` (pixi glue: keyboard→mover→direction→scene entity + camera follow + placeholder body/nose บอกทิศ) + config `PlayerConfig` (speed 4 tile/s, maxStepSeconds 0.1) + `runtime/app.ts` wire player เข้า ticker (dt วินาที). **ถอด debug pointer entity ออกจาก scene.ts** (player แทน) + ลบ `debugPointerEntity`/`debugEntity` config. เทสต์ใหม่ 3 ไฟล์ (167 เขียวรวม), lint/build ผ่าน. **ยังไม่ทำ**: sprite animation จริง (P0-06), click-to-move/pathfinding (P1), server sync (P0-07)
- 2026-07-12: P0-04 Renderer Scene Graph & Depth Sorting — `src/engine/render/{depth-registry,camera,scene}.ts` + `src/engine/runtime/app.ts` (rewrite) + config theme/camera knobs + `tests/engine-render-{depth,camera}.test.ts`. แยก pure logic (DepthRegistry = source of truth ของลำดับ + dirty tracking · camera math = bounds/clamp/lerp) ออกจาก pixi glue (scene.ts). Sort: assign unique zIndex rank จาก DepthRegistry → pixi sort เฉพาะเมื่อ dirty (ไม่ sort ทั้ง scene ทุก frame, TA §11); tie-break depthKey→tx→insertion seq (total order deterministic). Ground layer วาดครั้งเดียว (ไม่ sort). Camera: fixed iso no-rotation, lerp follow + clamp ขอบ map. **Cross-review fix (BLOCKER-1)**: lock convention "tile ที่ส่งเข้า entity/prop API = foot ต่อเนื่อง → `render/placement.ts` `entityFootToScreen` = tileToScreen (ไม่ +0.5)"; basis เดียวกับ depthKey/camera (กันเหลื่อมครึ่ง tile กับ cursor/กล้อง); prop integer ใน p0-test-field ปรับเป็น n+0.5 (author intent = กลาง cell); เพิ่ม known-traps 2 entry. เทสต์ 116 เขียว, lint/build ผ่าน
- 2026-07-12: P0-03 Test Map Config — `src/engine/map/{types,loader,p0-test-field}.ts` + `tests/engine-map-loader.test.ts`. MapConfig ตาม spec P0 §4.3 (field ชั้นนอกล็อก); CollisionLayer = blockedRects+blockedTiles → build blockedSet (packTile, lookup O(1) จาก integer tile); PropSpawn (tile float ได้ + zLayer สำหรับ depth band); MobPocket (area rect + packSize + activeCap ตาม TA §18). loader validate ด้วยมือ ไม่ใช้ zod (fail-loud, ชี้ field ผิด). P0 Test Field = 24×24, spawn (12.5,12.5), กำแพง+บ่อน้ำ block, 7 props (บาง float), 3 pockets (slime/mushroom). pure TS ไม่พึ่ง React/pixi
- 2026-07-12: P0-02 Isometric Coordinate System — `src/engine/iso/coords.ts` (TilePoint/ScreenPoint + tileToScreen/screenToTile/snapToTile) + `src/engine/iso/depth.ts` (depthKey + zLayer band) — pure math, ห้าม render/PixiJS; เทสต์ round-trip + band non-overlap (never-downgrade zone)
- 2026-07-11: สร้าง Next.js project + push ขึ้น GitHub
- 2026-07-11: นำ spec 6 ไฟล์เข้า repo (game spec v14 canonical + map bibles + tech architecture v1.4 + decision locks)
- 2026-07-11: ตั้งระบบ docs-for-AI (AI.md, CODEMAP, feature-map, context packs, decision-index, known-traps, path-guard test, agent personas)
- 2026-07-11: เพิ่ม specialist personas (engine/game/ui/qa/docs-curator) — realtime/worker/data/audio จดเป็น deferred รอ phase จริง (`.claude/README.md`)
- 2026-07-11: owner เคาะ 3 decisions: branch model (develop), ClickUp tracking, tech ตั้งเลข balance + update spec (ดู decision-index)
- 2026-07-11: นำภาพ ref 11 ภาพเข้า `docs/design/art-reference/` — กลุ่ม A (pixel art) = style target, กลุ่ม B = layout เท่านั้น
- 2026-07-12: owner สั่ง**เลิกใช้ ClickUp** — track งานใน repo ที่เดียว (docs); ไม่ลบข้อมูลใน ClickUp (decision-index #13/#16 superseded)
- 2026-07-12: import **spec v15 + tech v1.5 + P0_SCOPE_LOCK_v1**; ย้าย v14/v1.4 → `docs/history/`; อัปเดต reference ทุก index doc (canonical ชี้ v15/v1.5) — delta หลัก = P0 Scope Lock
- 2026-07-12: **P0-01 Runtime Foundation** — ติดตั้ง pixi.js v8.19 + สร้าง engine runtime (`src/engine/config.ts`, `src/engine/runtime/{app,resize,assets}.ts`) + `src/ui/GameCanvas.tsx` + route `/game`; unit test config+resize เขียว, build ผ่าน

## Blockers / owed

1. ~~ยังไม่มีโค้ดเกม / hold~~ — **owner สั่งเริ่ม P0 แล้ว 2026-07-12** — **P0-01→12 เสร็จหมดแล้ว 2026-07-12** (P0-12 handoff check ตรวจผ่าน 12/12) — รอ owner visual review บน browser + เปิด PR `feat/p0-engine-foundation` → `develop`. **P1-01→12 เสร็จหมดแล้ว 2026-07-12** (P1-12 handoff check ตรวจผ่าน 12/13 PASS + 1 PARTIAL, ดู Latest work) — รอ owner F3/F4 visual+perf review + เปิด PR `feat/p1-world-sync` → `develop`
2. ตัวเลข balance P1 (ค่า k, stat baseline, ตาราง skill 5 อาชีพ, mob Map 1) — **proposal ร่างแล้ว รอ owner เคาะ**: `docs/design/proposals/deungpu_P1_BALANCE_PROPOSAL_v1.md` (PENDING OWNER; ใช้ค่า draft ใน config ไปก่อน ไม่ block implement, เข้า spec ผ่าน §59.4 — ตอนนี้ P1 implement เสร็จด้วยค่า draft นี้ทั้งหมดแล้ว, นักดาบ 4 skills รันได้จริง). Checklist สิ่งที่ต้องเคาะอยู่ท้ายไฟล์ — จุดสำคัญ: k=50, resource pool (§50.1 มี resourceCost แต่ 10-stat ไม่มี pool), damageType physical/magic, อีก 4 อาชีพยังไม่มี skill data (config รองรับ แต่ไม่มี data ป้อน)
3. ~~ClickUp skill~~ — **ปิดแล้ว 2026-07-12** (เลิกใช้ ClickUp)
4. ~~PixiJS 8 ยังไม่ได้ติดตั้ง~~ — **ติดตั้งแล้ว 2026-07-12** (pixi.js v8.19, P0-01)
5. ภาพ ref กลุ่ม B (04–11) เป็น painterly — owner อาจ gen ใหม่เป็น pixel art มาแทน (ยังไม่กระทบ P0 — P0 ใช้ placeholder sprite generated ด้วยโค้ดทั้งหมด)
6. Colyseus server P0 เป็น local dev เท่านั้น (`npm run dev:server`, ยังไม่ deploy Render) — ตาม decision-index #20 พัฒนา local ต่อจนกว่า owner เคาะ deploy

## Owner decisions affecting immediate work

- Spec-first rule: ห้ามเดา ห้ามคิดเอง — เกิน spec ต้องอัปเดต spec ก่อน (decision-index #1)
- Locked decisions ทั้งหมด: tech architecture §0.1 (L1–L18) — server-authoritative, MySQL, Render, iso, 5 อาชีพ, P0 scope lock ฯลฯ

## Do not touch right now

- `docs/design/**` + `docs/tech/**` — spec แก้ได้เฉพาะ owner เคาะ

## Next recommended work

- **P1 complete — รอ owner review 2 PR**: (a) `feat/p0-engine-foundation` → `develop` (ค้าง review ตั้งแต่ P0-12) (b) `feat/p1-world-sync` → `develop` (ใหม่, ต่อจาก P0-12; ครอบ P1-01→P1-12) — **ต้องยืนยันกับ owner ก่อนเปิด/merge เสมอตาม AI.md**
- **สำคัญ — owner กด F3+F4 ตอน review**: เปิด `/game` (ต้องมี `npm run dev:server` รันด้วยเพื่อทดสอบ online path) แล้วกด **F4** toggle stress harness (~40 มอนสังเคราะห์ + ~300 damage number/วิ) → ดู FPS จาก debug overlay (**F3**) — ยืนยัน budget TA §11 (60fps desktop, Done Definition ข้อ 12) ด้วยตาเอง เพราะไม่มี headless WebGL renderer วัด FPS อัตโนมัติในเทสต์; ข้อ 1 (interpolation smooth)/9 (touch) ก็ verified by tests เท่านั้น ต้องลองจริงเหมือนกัน
- **เคาะ pending questions ค้าง** (`docs/tech/deungpu_P1_ISSUE_BREAKDOWN_v1.md` ท้ายไฟล์ + `deungpu_P1_BALANCE_PROPOSAL_v1.md`): ตาราง skill 5 อาชีพ + ค่า k (ตอนนี้ implement ด้วยค่า draft k=50/นักดาบ 4 skills ไปก่อนตาม decision-index 2026-07-12), ลำดับอาชีพถัดจากนักดาบ, city hub ตัดไป P2 หรือคงไว้ (implement แล้วใน P1-11 — ถ้า owner อยากตัดย้อนหลังแจ้งได้)
- P2 scope (ยังไม่เริ่ม, TA §12 phase plan): persistence/inventory/enhancement (TA §7-8), bot & report (TA §9), market (TA §5,§7), audio (TA §22), NPC/ร้านค้า/event เมือง (GS §3.3), Map 2+ (bible มี layout ยังไม่แปลง), mobile polish เต็ม (virtual joystick/HUD ปุ่ม)
- **P1 HUD**: ติดตั้ง Zustand จริง + ย้าย pattern poll ชั่วคราวของ P0-11 (`src/ui/DebugOverlay.tsx`) เข้า store bridge ตาม `docs/context/ui.md` (contract: UI คุยกับ game ผ่าน Zustand เท่านั้น)
