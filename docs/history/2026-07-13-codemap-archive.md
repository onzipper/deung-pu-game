# Archive: CODEMAP.md as of 2026-07-13 (pre-restructure)

> Verbatim copy taken before `docs/CODEMAP.md` was restructured into a thin EN orientation index (chore/agent-context-optimization, commit C7). Nothing below was edited.

# CODEMAP — file → responsibility

> structural only (path + 1-line responsibility) · test-enforced: every path referenced must exist (`npm test`)
> edit/move/delete/add a file ⇒ update its line in the same commit

## Next.js shell

- `src/app/layout.tsx` — root layout (font + globals)
- `src/app/page.tsx` — landing page (still default create-next-app)
- `src/app/game/page.tsx` — route /game: server shell → renders GameCanvas fullscreen
- `src/app/game/boot-gate.ts` — **pure DI** `resolveGameEntry(deps)` (owner-report round 2, Storage §5/§5.3): checks fresh data from the session+characters API (see src/app/api/auth/session/route.ts, src/app/api/characters/route.ts) before every mount (no longer trusts the stale hub-time `lastMapId`) → `{action:"mount"}` (also syncs `rememberSelectedCharacterMapId`/clears the map key) or `{action:"redirect-hub"}`; every fetch failure/network error = best-effort mount (keeps the dev/offline solo flow working) — tests: `tests/app-game-boot-gate.test.ts`
- `src/app/globals.css` — Tailwind v4 entry + theme vars
- `src/app/favicon.ico` — favicon

## Shared (client ↔ server)

- `src/shared/net-protocol.ts` — realtime wire contract (P0-07, no runtime dep): MAP_ROOM_NAME/DEFAULT_MAP_ID/MSG_MOVE + MoveMessage/PlayerSnapshot/WirePlayerDirection/Anim; **P1-08**: JoinOptions(mapId+**partyId**, channelId removed — server auto-assigns) · PlayerSnapshot.**partyId** · CHANNEL_LABEL_PREFIX/`channelLabel(n)`/DEFAULT_CHANNEL_ID(=CH.1 server-assigned label)/DEFAULT_PARTY_ID(="") — importable from both the client (alias @/shared) and the server (relative path); **P1-02**: MSG_POSITION_CORRECTION + PositionCorrectionMessage{tx,ty,direction,anim,reason}; **P1-03**: MobSnapshot{mobId,mobType,tx,ty,state,hp}/WireMobState; **P1-05**: MSG_CAST_SKILL(+CastSkillMessage{skillId,aimTx,aimTy,direction}) · MSG_SKILL_RESULT(+SkillResultMessage{casterId,skillId,hits:SkillHit[]}) · MSG_CAST_REJECTED(+CastRejectedMessage) — MSG_DEBUG_KILL_MOB removed; **P1-10**: MSG_MAP_TRANSITION(+MapTransitionMessage{exitId,targetMapId,targetSpawn}) — server commands the map crossing (§57.3 separated rooms) — _note P1-11: zoneType is **not** on the wire; both client/server derive it from the map config in the registry_
- `src/shared/reconnect.ts` — **pure** reconnect logic (P1-07, GS §59.1 · TA §6, no runtime dep): `decideReconnect({withinGrace,roomOpen,positionValid})→"resume"|"safe_camp"` (§59.1 decision table) · `resolveSpawnPosition(requested,safeCamp,isWalkable)→{pos,usedSafeCamp}` (invalid/non-finite → safe camp) · `reconnectBackoffMs(attempt,cfg)` exponential+cap · `shouldRetryReconnect(attempt,cfg)`; **P1-07-fix cross-reload**: `StoredReconnectRecord`{token,savedAtMs,serverUrl,mapId,partyId} + `planRejoin(stored,ctx)→{action:"reconnect",token}|{action:"fresh"}` (reconnect only when the token is fresh+matches server/map/party+age<grace) + `parseStoredReconnect(unknown)→record|null`; imports type ReconnectClientRetryConfig from @/engine/config
- `src/shared/movement-validation.ts` — **pure** validateMove(prev,next,elapsedMs,params,isWalkableAt) (P1-02, TA §6/§16.3): server-authoritative move check → {ok} | {ok:false,reason,correctTo=prev}; order non_finite→teleport(absolute cap)→speed(clamp elapsed min/max against clock skew, no division)→blocked; walkable check goes through a callback (the caller composes the engine's snapToTile+isWalkableTile — doesn't copy the formula); knob from @/engine/config (type-only)

## Game engine (P0)

- `src/engine/config.ts` — shared config/types (EngineConfig, DEFAULT_ENGINE_CONFIG, tileSize 64×32, SceneTheme/CameraConfig/PropStyle/PlayerConfig/PlayerAnimationConfig+PlayerSpriteStyle, **MobConfig** P0-09 (spawn/wander/animation/styles) + **P1-03** MobAiConfig (tickHz/chaseSpeed/aggroRadius per type/leash/deaggro/pullCap)+MobLodConfig (aoiRadius/idleTickHz)+respawnDelayMs, **NetConfig** P0-07 (**P1-08**: channelId→**partyId** knob + **channelCapacity/partyChannelCapacity** server knob auto-assign §59.3), **P1-07 ReconnectConfig** (graceSeconds §59.1 + clientRetry backoff — mirrored client/server + **P1-07-fix** sessionStorageKey per-tab cross-reload token), **CombatStubConfig** P0-10 (attack/dummyDamage offline/hitboxDebug/deathFeedback — damageNumber moved to combatFeel), **P1-05** **CombatBalanceConfig** (k/player stat/mob stat per type — server, PENDING OWNER) + MobConfig.**hpBar** (client visual), **P1-06** **CombatFeelConfig** (DamageNumberPoolConfig pool/normal/crit/aggregate style + aggregateWindowMs · HitStopConfig durationMsByLevel/timeScale/**minLevelOnKill/minLevelOnCrit** (feel-floor follow-up) · ScreenShakeConfig levelsByLevel/enabled/alwaysTriggerAtLevel/**minLevelOnKill/minLevelOnCrit** · EffectQualityConfig current+tiers Low/Med/High/Cinematic) + **StressHarnessConfig** (F4 toggleKeyCode/syntheticMobCount/damageNumberRatePerSec/maxSpawnPerTick, dev-only), **DebugOverlayConfig** P0-11, **P1-09** **PathfindingConfig** (maxSearchNodes/arrivalRadius/replanOnBlock/clickMobPickRadius/marker style — click-to-move + touch L11), **P1-10** **TransitionConfig** (fadeColor/fadeOutMs/fadeInMs — map transition fade §57.3), **P1 fix** **ExitMarkerConfig** (enabled/fill/line color·alpha·width — highlights the exit area floor)) — every tunable value lives here (mob HP = combatBalance is the single source of truth)
- `src/engine/runtime/app.ts` — createEngine(): wraps the pixi Application (async init) + mounts the P0 Test Field scene + local player + **mob view manager (P1-03, `src/game/mob/manager.ts`)** + **mob source controller** (switches between server-driven online / offline local-sim fallback based on net status) + **combat coordinator (P0-10→P1-05→P1-06: clientView skill for the first swordsman + castSkill/isOnline wiring, onSkillResult→combat, hit-stop/shake handled internally by combat-stub)** + **stress harness (P1-06 §5, dev-only)**: F4 keydown toggle (knob: stressHarness.toggleKeyCode) → `stressHarness.update()` inside the ticker + cleanup on destroy + **net layer (P0-07, offline-safe; joinOptions mapId+channelId P0-08)** + **pointer tile tracking (P0-11)** + **click-to-move + touch (P1-09→P1-09.1, L11)**: pointerdown (mouse-left/tap) → footFromEvent → tapping a mob within clickMobPickRadius = `startEngage(mobId)` (`src/game/combat/target-engage.ts`, pure state machine), then `updateEngage()` every frame attacks **continuously** until the target dies/disappears, replans a chase when the target moves out of range, cancels immediately on `manualInputActive` (WASD) or clicking elsewhere / tapping the floor = player.moveTo (A*) + cancelEngage + ticker + fps ui + destroy(); **P0-11**: `getDebugInfo()` + `setDepthDebug()` passthrough → scene; **P1-10 multi-map + transition (§57.3)**: boot map from the registry (**owner-report#6 fix**: `pickBootMapId` picks the map the selected character last saved on, if any + the registry knows it, else DEFAULT_MAP_ID); world = a per-map unit (`mountWorld(map,spawn)` builds scene+player+mobs+combat+net+input + **P1 fix** scene `setExitMarkers` highlights the exit-area floor, clean teardown on map crossing) — the outer shell (app/ui/fps/resize/transition/master tick) stays fixed, scene/player/net are live getters; `requestTransition` → fade → **owner-report#6 fix**: `rememberSelectedCharacterMapId(targetMapId)` (written before the swap succeeds) → destroys the old world (net leave=consented) → mounts the destination world at targetSpawn → fade in (input lock); online = server-commanded (onMapTransition) · offline = the client checks findExitAt itself
- `src/engine/runtime/transition.ts` — pixi glue (P1-10): fade overlay (a Graphics covering the whole screen, topmost in the stage) + `createTransitionController(parent,config,w,h)` wraps the state machine → start(swap)/update(dtMs)/isLocked/resize/destroy; the swap callback fires when the screen is fully dark (teardown+rebuild world)
- `src/engine/runtime/transition-state.ts` — **pure** (P1-10, §57.3): fade/lock state machine idle→fadeOut→[SWAP]→fadeIn→idle · `advanceTransition(state,dtMs,timing)→{state,alpha,fireSwap}` · startTransition/idleTransition/isTransitionLocked — testable without pixi
- `src/engine/runtime/resize.ts` — attachResize(): a ResizeObserver on the container → renderer.resize; clampSize (pure)
- `src/engine/runtime/assets.ts` — asset loader stub (wrapper around pixi Assets, empty manifest)
- `src/engine/runtime/debug-info.ts` — **pure** (P0-11): the EngineDebugInfo shape + `buildDebugInfo()`/`roundTile()` (rounds playerTile to 2 decimal places) + `IDLE_NET_DEBUG_INFO` (net.enabled=false fallback) — doesn't touch pixi/colyseus, directly testable
- `src/engine/input/keyboard.ts` — keyboard intent tracker: MOVE_KEYS (WASD+arrows) + intentFromKeys (screen basis → tile-space, pure) + attachKeyboard (listeners + detach) + **ATTACK_KEY (Space) edge-triggered consumeAttackPressed() P0-10**
- `src/engine/movement/mover.ts` — stepMovement (pure): continuous float-tile movement + diagonal normalize + axis-separated collision slide + dt clamp
- `src/engine/movement/direction.ts` — resolveDirection (pure): tile vector → 8-dir logical direction (computed from the on-screen angle) + directionToScreenUnit — groundwork for P0-06's 5-dir+mirror sprite
- `src/engine/movement/path-follower.ts` — advancePathFollower (**pure**, P1-09): follows A* waypoints using the same stepMovement (same speed/collision as WASD) — aims at cell centers (n+0.5), reaches (arrivalRadius)→next waypoint, done→arrived; dynamic obstacle (target blocked/stuck)→blocked (the controller decides whether to replan/stop); empty waypoints = arrived (the path-cancel route)
- `src/engine/pathfinding/astar.ts` — findPath (**pure**, P1-09, TA §17.3): A* on an integer iso grid, 8-dir neighbors (diagonal blocks corner cutting — both adjacent ortho cells must be walkable), octile heuristic, MinHeap open set, maxSearchNodes cap (returns null when exceeded), walkable via a callback (isWalkableTile) — returns integer-cell waypoints (excluding start), []=already arrived, null=unreachable/goal blocked/cap exceeded
- `src/engine/player/local-player.ts` — createLocalPlayer(): pixi glue connecting keyboard→mover→direction→animator→scene entity + camera follow; walk/idle + 5-dir+mirror animated sprite (replacing the body+nose placeholder); exposes position/facing/**animation** for net sync; **P0-10**: triggerAttack()/isAttacking (locks animation="attack" until the clip finishes) + consumeAttackPressed() forwarded from keyboard; **P1-02**: applyCorrection(tx,ty) snaps position+camera from the server (reconcile); **P1-09** click-to-move: moveTo(goal)→finds a path via A*→walks it via path-follower + a fading destination marker; a WASD press interrupts = manual override cancels the path; replanOnBlock; walk-to-attack helpers: faceToward(tile)/requestAttack() (also wired to Space)/isFollowingPath/cancelPath; **P1-09.1**: `manualInputActive` getter (true only on frames with WASD intent) — app.ts uses it to cancel target-engage state immediately (manual override always wins); **prod fix 2026-07-12**: applyCorrection no longer drops the path — replans to the same goal (planCorrectionResume) and keeps walking
- `src/engine/player/correction-resume.ts` — planCorrectionResume (**pure**, prod fix 2026-07-12): decides, after the server snaps the position (correction/self-spawn), whether to resume walking to the same goal — reuses findPath (A*): `goal=null`→"idle" (WASD/fresh join) · can still reach it→"walk"+waypoints (resume) · already there/unreachable→"stop" (cancel). Used by both applyCorrection + replanToGoal (local-player.ts)
- `src/engine/net/sync.ts` — net sync **pure** helpers (P0-07, computePlayerCount P0-08): coerceDirection/coerceAnim + snapshotChanged (anti-spam) + advanceSendTimer (throttle) + toMoveMessage + computePlayerCount (debug overlay) + ConnectionState type (**P1-07**: adds "reconnecting") + **canSendLocalMove(state,selfAdopted)** (fix for issue #1/#2: gates sendMove until self's authoritative position has been adopted — prevents sending the first move from spawn before knowing the server's held position)
- `src/engine/net/party.ts` — **pure** party id resolution (P1-08, GS §59.3): `resolvePartyId(search,fallback)` reads `?party=xyz` from the query string (trim, empty→fallback) + `partyIdFromLocation(fallback)` (guards no-window) — dev sets a test partyId to try party sync (invite/leader UI = P2)
- `src/engine/net/net-client.ts` — createNetClient(): colyseus.js glue — connect/joinOrCreate MapRoom (joinOptions mapId+**partyId** P1-08, no channelId sent) + wires schema callbacks → onPlayerAdd/Change/Remove + sendMove; NetStatus/**getNetDebugInfo()** reads **channelId (server-assigned) + partyId** from state (P1-08 overlay); **graceful offline** (a failed connect = solo); NetStatus + **getNetDebugInfo()** (P0-08); **P1-02**: onMessage(MSG_POSITION_CORRECTION) → correctionCount + onPositionCorrection (reconcile); **P1-03**: wires state.mobs onAdd/Change/Remove → onMobAdd/Change/Remove(MobSnapshot); **P1-05**: sendCast(CastSkillMessage) + onMessage(MSG_SKILL_RESULT→onSkillResult, MSG_CAST_REJECTED→onCastRejected); **P1-07 reconnect** (§59.1): stores the reconnectionToken → onLeave code≠4000 (unintentional drop) = beginReconnect (backoff retry back into the same seat → silently re-wires, resetTrackedEntities to prevent duplicate remote/mob entries) · out of attempts = freshJoin (safe camp) · status "reconnecting" · config.retry knob — sendDebugKillMob removed; **P1-07-fix cross-reload**: config.store (ReconnectStore) + config.graceSeconds — persistToken() on every wire (memory + per-tab store) + pagehide/beforeunload re-persist the timestamp (not leaving = letting it drop unconsented so it can be reclaimed) · boot() = planRejoin(store.load): a fresh token → tries `client.reconnect(token)` first (reclaims the old seat), on failure → clear+freshJoin · disconnect() (consented) = store.clear()+detaches pagehide; **P1-10**: onMessage(MSG_MAP_TRANSITION → onMapTransition handler); **fix issue #1/#2 (self-adopt)**: wire() resets `selfAdopted=false` → the players.onAdd self branch → sets `selfAdopted=true` + fires `onSelfSpawn(snap)` (the caller snaps the local player/camera to the authoritative position: fresh join=spawn / reconnect=held position) · sendMove is gated by canSendLocalMove (must not send before adopting) — fixes "warps back to the same spot while walking" + "stepping on the exit marker doesn't cross the map (while online)"
- `src/engine/net/reconnect-store.ts` — **P1-07-fix**: an adapter that stores the reconnection token across page reloads (per-tab): `ReconnectStore`{load/save/clear} + `createSessionReconnectStore(key)` (sessionStorage — **not localStorage**, which would have 2 tabs fighting over the token) falling back to in-memory if there's no window + `createStorageReconnectStore(storage,key)` (inject a KeyValueStorage → testable without a DOM) + `createMemoryReconnectStore()`; parse/validate = shared/reconnect's parseStoredReconnect
- `src/engine/net/interpolation.ts` — createInterpolationBuffer(): **pure** per-entity snapshot ring buffer (P1-01, TA §6) — push(t,tx,ty,dir,anim) + sampleAt(renderTime) lerps position from the 2 bracketing snapshots; edge cases: empty→null, newly-created→clamp, starved→short extrapolate+clamp; out-of-order→drop; preallocated slot/result (no `new` in the hot loop)
- `src/engine/net/remote-player-manager.ts` — createRemotePlayerManager(): pixi glue that creates/moves/removes remote player entities (a differently-colored animator) from net events; **P1-01**: pushes snapshots into the interpolation buffer (clock injectable) → the ticker's sampleAt(now−bufferMs) → renders slightly in the past, smoothly (replacing the old P0-07 simple lerp); **owner-fix**: `playAttack(sessionId)` — plays the remote's attack clip event-driven (the wire anim never carries "attack") via remote-attack.ts's timer, overriding the sampled anim temporarily inside update()
- `src/engine/net/remote-attack.ts` — **pure** (owner-fix "we can't see other players attacking on our screen"): createRemoteAttackState/triggerRemoteAttack/advanceRemoteAttack — a timer that locks the remote's attack clip (same attackFrameDuration×attackFrames as local-player.ts), checked before advancing time (still true on the exact frame the clip triggers/finishes)
- `src/engine/animation/manifest.ts` — animation manifest (data-driven) + resolveClip (pure): logical direction → sprite source+mirror flag (5-dir+mirror, 8-dir override L15) + advancePlayhead (pure frame timing/loop) + createPlayerAnimationManifest
- `src/engine/animation/player-placeholder.ts` — generatePlayerTextures(): draws a placeholder sprite in code (Graphics→RenderTexture) — 5 directions, walk/idle/attack, asymmetric so the mirror is visible; a fixed foot anchor
- `src/engine/animation/animator.ts` — createSpriteAnimator(): pixi glue playing frames on a Sprite (setState/update) + mirrors via scale.x=−1 around the foot anchor
- `src/engine/iso/coords.ts` — iso projection converters: TilePoint/ScreenPoint types + tileToScreen/screenToTile/snapToTile (pure math)
- `src/engine/iso/depth.ts` — iso depth sort key: depthKey(tile, zLayer) + band constants (pure math)
- `src/engine/map/types.ts` — MapConfig schema (spec P0 §4.3) + CollisionLayer/PropSpawn/MobPocket (+**P1-03** optional respawnDelayMs override) + packTile/isBlockedTile/isWithinBounds/isWalkableTile helpers; **P1-07**: optional `safeCamp` (§59.1 reconnect fallback) + `safeCampOf(map)` (= safeCamp ?? spawnPoint); **P1-10**: `MapExit`{exitId,area,targetMapId,targetSpawn} + MapConfig.exits field + `isTileInRect`/`findExitAt` (pure, mirrors client/server exit detection §57.3); **P1-11**: optional `zoneType:"safe"|"field"` (GS §14, default field) — `MapZoneType` defined in config.ts
- `src/engine/map/loader.ts` — loadMapConfig(raw): validates + builds the blockedSet (MapConfigError, no zod); **P1-07**: validates the optional safeCamp the same way as spawnPoint (within bounds + walkable); **P1-10**: parses optional exits (intrinsic checks: unique id/area within bounds/targetMapId+targetSpawn shape — cross-refs = registry); **P1-11**: parses optional zoneType (enum "safe"|"field", default "field")
- `src/engine/map/registry.ts` — **pure, shared by client+server** (P1-10/P1-11, §57.3): mapId→MapConfig (p0-test-field, map1, city-hub) built once, fail-loud · `validateMapCrossRefs` (exit.targetMapId actually exists + targetSpawn is walkable at the destination) · `getMap`/`requireMap`/`hasMap`/`buildMapRegistry`(fixture) — the single source of truth for which maps exist
- `src/engine/map/map1.ts` — Map 1 "the edge of the human city" config (P1-10, MAP_LAYOUT_BIBLE §Map 1 + MAP_SCALE §Map 1): 40×40 Small–Medium, 4 pockets (slime C/bird E/boar SW/elite S per the density in §6), placeholder art/collision — `MAP1_ID="map1"`; **P1-11**: N city gates → city-hub (the production target per the bible, replacing the dev wire to p0-test-field)
- `src/engine/map/city-hub.ts` — **P1-11** City Hub "นครอรุณผนึก" (Sealed Dawn City) config (GS §3.3 live hub · §14 Safe Zone): 32×32 center, `zoneType:"safe"` (no combat), **no mobPocket**, landmark props placeholders (Sealed Temple/blacksmith street/hunters' guild/news board), a south gate ↔ map1 — `CITY_HUB_ID="city-hub"`; NPCs/shops/events = P2+
- `src/engine/map/p0-test-field.ts` — P0 Test Field config data (24×24, spawn/collision/props/3 pockets); **P1-10**: a dev exit on the north edge → map1 (the dev boot map)
- `src/engine/render/depth-registry.ts` — DepthRegistry<D> (pure): entity registry + compareDepth comparator + dirty-tracked sorted() — the source of truth for depth ordering
- `src/engine/render/camera.ts` — camera math (pure): computeMapScreenBounds / clampCameraScreen / lerpTile (fixed iso, no rotation/zoom) + **P1-06**: `applyShakeOffset(point, offset)` (adds the screen-shake offset to the camera, always applied after clamping)
- `src/engine/render/placement.ts` — entityFootToScreen (pure): locks in the convention "the tile passed into the API = a continuous foot position → tileToScreen (no +0.5)"
- `src/engine/render/exit-marker.ts` — **pure** (P1 fix): tileDiamondPolygon/buildExitMarkerPolygons — converts map.exits[].area into a diamond polygon (screen space, reusing the same tileToScreen as buildGround) so the scene can draw a highlight on the exit-area floor (the owner couldn't find the exit because of the placeholder art); doesn't touch pixi/depth-sort
- `src/engine/render/object-pool.ts` — createObjectPool<T>() (**pure**, P1-06, TA §11): a generic object pool — acquire()/release()/activeCount/capacity/createdCount; the factory only runs up to capacity, then recycles existing objects (zero-alloc at steady state); full → acquire() returns undefined, releasing a foreign/duplicate object = a safe no-op — kept separate from pixi glue (game/combat/damage-number.ts uses this pool with BitmapText) so it's testable without WebGL
- `src/engine/render/screen-shake.ts` — screen shake state/decay (**pure**, P1-06, GS §17.5): createShakeState/triggerShake (level→amplitude×scale/duration, a stronger new shake replaces the old, a weaker one doesn't override)/advanceShake (real-time decay)/computeShakeOffset (linear decay + a random direction from an injectable RandomFn) — lives in the engine layer, doesn't import src/game/**
- `src/engine/render/scene.ts` — createMapScene(): pixi glue — a ground layer (grid/checker/blocked, drawn once) + an entity layer (depth-sorted via zIndex rank) + fixed camera follow + entity API (addEntity/moveEntity/removeEntity) + an `entityCount` getter + **P0-11**: `setDepthDebug()` — a depth-rank text layer (a sibling of entityLayer, synced during syncDepth when enabled, fully removed when disabled/destroyed) + **P1-06**: `setCameraShakeOffset(offset)` — stores the latest offset, applyCamera() adds it to the camera **after** clamping, always (camera.ts's applyShakeOffset) + **P1 fix**: `setExitMarkers(polygons,style)` — an exitMarkerLayer (above ground, below entityLayer, not depth-sorted) that draws the exit-area floor highlight (geometry from exit-marker.ts)

## Game logic (P0-09, on top of the engine — src/game/**)

- `src/game/mob/rng.ts` — RngFn type + defaultRng (Math.random) + createLcgRng (seeded deterministic, used in tests) — injectable at every point that rolls randomness
- `src/game/mob/spawn.ts` — spawnPocketMobs/spawnAllPockets (pure, TA §18.1 fixed pocket + random point inside): count = random(packSize) clamped to activeCap, spawn point = findWalkableSpawnPoint (retries per MobSpawnConfig.maxPlacementAttempts, best-effort skip if none found)
- `src/game/mob/wander.ts` — createWanderState/stepWander (pure): toggles idle/walk on random intervals from MobWanderConfig, walks via the same stepMovement + a simple leash (uses pocket.area as an extra blocking condition) + a walkableFromMap helper
- `src/game/mob/manifest.ts` — createMobAnimationManifest(): reuses the engine's AnimationManifest/resolveClip — 2 directions actually drawn (S,N) + mirrored for the other 6 (mobs are symmetric, see the file for the reasoning)
- `src/game/mob/placeholder.ts` — generateMobTextures(): a code-drawn placeholder (Graphics→RenderTexture) per mobType (a green bouncy slime blob / a mushroom with a red cap), generated once per mobType (shared across instances)
- `src/game/mob/ai.ts` — **pure** mob AI decisions (P1-03, TA §18.3/§6/§11): selectAggroTarget (nearest within radius + respects the pull cap) · shouldReturnToSpawn/hasReachedSpawn (leash) · stepToward (chase/return via stepMovement) · isPocketActive/idleTickInterval/shouldStepPocket (AI LOD) · isRespawnDue (timer) · distSq — doesn't touch pixi, shared by both server and offline
- `src/game/mob/simulation.ts` — createMobSimulation(): **authoritative stateful sim** (P1-03, pure TS) — the initial spawn (spawnAllPockets) + one-at-a-time respawn on killMob (respawnDelayMs, injectable clock) + AI tick (wander/aggro/leash via ai.ts) + AI LOD (pocket active/asleep). tick(dt,players,now)/killMob/**damageMob(id,amount)→{hp,killed} P1-05 combat death**/forEach/snapshots/aggroCountFor — the server (MapRoom) runs this authoritatively, the client's offline fallback runs the exact same one
- `src/game/mob/manager.ts` — createMobViewManager() (**P1-03 refactor from P0-09**): pixi glue, **view only** — renders/moves/removes mobs from a snapshot (server state or the offline sim) through the **interpolation buffer** (P1-01, a mob is an entity just like a remote player); onMobAdd/Change/Remove + syncAll (offline bulk) + removeAll + getAliveTargets (combat) + facing derived from delta (direction isn't synced); **P1-05**: a small HP bar above any mob with hp < maxHp (maxHp from combatBalance, a container wrapping the animator+bar); no game logic here (authority lives in simulation.ts)

## Game logic (P1-04, skill schema — src/game/skill/**)

- `src/game/skill/types.ts` — the `SkillDefinition` type: 37 fields matching the names/order of **GS v15 §50.1 exactly** (must not rename) — a comment explains the type for any field whose interpretation is ambiguous (citing §50.2–50.4 + the proposal) + `SKILL_FIELD_NAMES` (a readonly tuple of the 37 names, deriving `SkillFieldName`) — used by both the loader (the unknown-field guard) and the tests
- `src/game/skill/loader.ts` — `loadSkillDefinitions(raw: unknown[]): Map<skillId, SkillDefinition>` (pure, not zod, matches the pattern of `src/engine/map/loader.ts`) — fail-loud: a missing field/wrong type/an invalid negative value/an unknown field/a duplicate skillId → `SkillDefinitionError` naming the field path; **casting itself isn't implemented yet (P1-05 scope)**
- `src/game/skill/views.ts` — splits fields by runtime role (**TA §16.1**): `serverView(def)` (a shallow copy of all 37 fields) · `clientView(def)` + the `ClientSkillView` type (strips 9 server-only fields: baseMultiplier/scalingStat/damageType/maxTargets/hitCount/bossModifier/pvpModifier/crowdControl/serverAuthority — "must not ship into the client bundle") · the `SERVER_ONLY_FIELDS` const — a trailing comment records a gap where §16.1 doesn't clearly classify skillName/description/statusEffects (kept in clientView for the documented reasoning)
- `src/game/skill/data/warrior-skills-server.ts` — **SERVER-ONLY** data for the swordsman's 4 skills (the full 37-field SkillDefinition, including server-only literals like baseMultiplier/bossModifier/maxTargets etc.) copied from proposal §3.1 — **PENDING OWNER**; `WARRIOR_SKILLS_SERVER` is fed into loadSkillDefinitions (server/**+tests). **The client must not import this** (the literals leak into the bundle — see known-traps)
- `src/game/skill/data/warrior-skills-client.ts` — **CLIENT-SAFE** manifest for the swordsman's 4 skills (ClientSkillView, 28 fields, **none of the 9 server-only fields, not even as literals**) — `WARRIOR_SKILLS_CLIENT` is what the client (app.ts/HUD) imports directly; a drift guard requires it to equal clientView(server) exactly (tests/game-skill-loader.test.ts)

## Game logic (combat — src/game/combat/**)

- `src/game/combat/hit-test.ts` — pure combat geometry (doesn't touch pixi, **shared client/server**): findHits (tile-space radius + a screen-space arc around facing + an optional **HitTolerance**: rangePadding/arcPadding/pointBlank to prevent "attacks not landing" from interp lag, P1-05.1 — default ZERO=the old behavior) + ZERO_HIT_TOLERANCE, rollDummyDamage/advanceCooldown/canAttack/applyDummyDamage (offline/stub) + helpers screenAngleForDirection/tileUnitVectorForScreenAngle (reused in combat-stub.ts + cast-validation.ts). The HitTolerance type is defined in engine/config.ts (the source of the knob)
- `src/game/combat/formula.ts` — **PURE + SERVER-ONLY** (P1-05 + P2-14, TA §15.2/§15.7.1): computeDamage/computeSkillDamage (the diminishing-returns formula ATK×baseMult×mitigation[k/(k+effDef)], crit, bossModifier/pvpModifier/tierReduction) + **multi-hit rounding = round the total once, then distribute back to sub-hits via deterministic largest-remainder (§50.1.1 item 2 — invariant: sum(subHits)===damage, never-downgrade)** + effectiveDef/mitigationFactor (guards div-by-0/negative); RNG is injected (1 call per sub-hit); **must never be imported into the client bundle** (see known-traps)
- `src/game/combat/cast-validation.ts` — **pure** (P1-05, TA §16.2/§16.3/§18.4): validateCast (safe_zone/unknown_skill/cooldown/out_of_range — **P1-11** safe_zone is checked first of all: the city rejects every cast, GS §14) + isSkillReady/skillReadyAt + isAimInRange + skillAttackShape (§50.1→geometry) + resolveSkillHits (findHits + the maxTargets cap, nearest-first; accepts an optional HitTolerance forwarded to findHits — P1-05.1 anti-miss)
- `src/game/combat/damage-aggregate.ts` — a damage-number aggregate window (**pure**, P1-06, GS §17.10 · TA §11): createDamageAggregateState/addToAggregate (accumulates a total per bucket key, e.g. mobId)/tickDamageAggregate (flushes as a single combined number every windowMs) — used when damage-number.ts exceeds its budget/quality cap
- `src/game/combat/hit-stop.ts` — hit-stop state/timescale (**pure**, P1-06, GS §17.5): createHitStopState/triggerHitStop (level→duration; a new trigger doesn't stack, uses whichever is larger)/advanceHitStop (real-time decay)/computeHitStopTimeScale — **visual only** (combat-stub.ts only uses the scale for juice updates, never touches network/sim/cooldown)
- `src/game/combat/damage-number.ts` — createDamageNumberLayer() (**P1-06 refactor from P0-10**): pixi glue — **BitmapText + an object pool** (engine/render/object-pool.ts, poolSize 300–500 knob) instead of creating/discarding Text; separate normal/crit styles (different BitmapFont names, bigger+different color, GS §17.3); the layer is the scene.world's only child (a public field) (not routed through scene.addEntity to avoid churning the DepthRegistry); over budget/quality cap → routed into damage-aggregate.ts (flushed as one number every 0.5s, GS §17.10); an `activeCount` getter (debug/stress harness)
- `src/game/combat/stress-harness-rate.ts` — computeStressSpawnBatch() (**pure**, P1-06 §5): the same kind of accumulator as net/sync.ts's advanceSendTimer but returns a spawn count per tick (supports high rates, 300/s) + a cap to prevent spikes when dt jumps
- `src/game/combat/stress-harness.ts` — createStressHarness() (**DEV-ONLY**, P1-06 §5, TA §11 budget proof): F4 toggle → spawns ~40 synthetic mobs directly via MobViewHandle.onMobAdd (prefixed `stress:`) + ~300 damage numbers/s via CombatStubHandle.spawnSyntheticDamageNumber (the real pool/aggregate, not a separate path) — the owner presses F4 and watches FPS on the debug overlay (F3) themselves, no automated FPS assertion
- `src/game/combat/combat-stub.ts` — createCombatStub(): the client combat coordinator (**P1-05 server-authoritative → P1-06 combat feel**) — Space→cooldown gate→triggerAttack() anticipation immediately→**online**: sends a castSkill intent (net) + onSkillResult(result, isOwnCast)→a damage number from the **server's result** for every caster (+ hit-stop/screen-shake triggers **only when isOwnCast**, on crit/kill or a high skill.screenShakeLevel, the effective level via `resolveJuiceLevel` — owner-fix: a friend killing a mob shouldn't shake our own screen — P1 scope: a single skill = deps.skill); **offline**: a dummy damage number (a non-authoritative playground, doesn't actually kill) + a hitbox debug wedge (the real skill's shape); **P1-06**: hit-stop timescale only for juice updates (damage number/hitbox fade, never touches cooldown/network) + pushes the screen-shake offset into `scene.setCameraShakeOffset()` every frame (real-time decay) + `spawnSyntheticDamageNumber()` (dev-only, the stress-harness hook). **Doesn't import formula.ts** (server-only)
- `src/game/combat/juice-level.ts` — resolveJuiceLevel() (**pure**, P1-06 follow-up, GS §17.5): the effective hit-stop/screen-shake level = max(skill.baseLevel, minLevelOnKill if killed, minLevelOnCrit if crit) — a knob-driven floor preventing a low-level skill (e.g. S1 sword_basic_slash=0) from making a kill/crit feel like nothing (doesn't touch skill data — combatFeel.hitStop/screenShake.minLevelOnKill/minLevelOnCrit are new)
- `src/game/combat/target-engage.ts` — the target-engage state machine (**pure**, P1-09.1 follow-up, TA §17.3 walk-to-attack): `IDLE_ENGAGE_STATE`/`startEngage(id)`/`cancelEngage()`/`stepEngage(ctx)` → attacks continuously while the target is in range (doesn't end after one hit) / chases when the target moves out of range and there's no path already leading there / goes idle when the target dies or disappears — app.ts executes the actual actions (moveTo/faceToward/requestAttack/cancelPath) + cancels immediately on `manualInputActive` (WASD always wins)

## Realtime server (P0-07, a separate process — L4)

- `server/index.ts` — the Colyseus Server entry point: defines MapRoom **+ `.filterBy(['mapId','partyId'])`** (**P1-08** auto-assign + party sync §59.3 — a solo pool + party affinity; channelId is no longer a filter key) + listens on ws://localhost:2567 (env PORT) — run via `npm run dev:server` (tsx)
- `server/matchmaking/channel-registry.ts` — a **pure** channel-number allocator (P1-08, GS §59.3): createChannelRegistry() → assign(mapId) = the lowest free number → "CH.n" · release(mapId,channelId) reuses the number · channelNumbersFor (debug); a module-level singleton inside MapRoom, **single-process** (Redis = scaling, TA §8 TODO) — a display label only (real auto-assignment = Colyseus filterBy+maxClients)
- `server/rooms/MapRoom.ts` — MapRoom (a map+channel instance): onCreate/onJoin/onMessage/onLeave/**onDispose**; **P1-08 channel auto-assign** (§59.3): onCreate reads partyId from options → maxClients = channelCapacity (solo) / partyChannelCapacity (party) → auto-locks at the cap = auto-assigns a new channel; channelRegistry.assign → state.channelId (server-assigned) · onJoin sets PlayerState.partyId · onDispose releases the channel · cap overridable via env CHANNEL_CAPACITY/PARTY_CHANNEL_CAPACITY (dev/test); **P1-02 movement**: validateMove on every MSG_MOVE → snaps back on failure (MSG_POSITION_CORRECTION); **P1-03 mob sim**: createMobSimulation + setSimulationInterval(10Hz) → stepMobSim → syncMobsToState (**AOI filter §18.2** = TODO); **P1-05 server combat authority**: onCreate loads skills (loadSkillDefinitions WARRIOR_SKILLS) + the combatBalance knob + per-player cooldown state; handleCast(MSG_CAST_SKILL) → validateCast → resolveSkillHits (maxTargets cap) → computeSkillDamage (the server formula §15.2) → sim.damageMob (death→despawn+respawn) → sync + broadcast MSG_SKILL_RESULT · rejection → MSG_CAST_REJECTED; **P1-07 reconnect grace** (§59.1): onLeave(consented?) splits into consented (removed immediately) vs dropped (allowReconnection holds for `graceSeconds` → reconnecting in time restores the same state/tracker/cooldown, expiring → removePlayer) · onJoin uses resolveSpawnPosition to snap invalid coordinates→safe camp; graceSeconds = a knob (env RECONNECT_GRACE_SECONDS overrides for dev/test); **P1-10 map transition (§57.3)**: onCreate loads the map from the **registry** (requireMap by state.mapId — not hardcoded to the test field) · checkExit runs on every valid MSG_MOVE (findExitAt → fires MSG_MAP_TRANSITION the instant it's entered, tracker.lastExitId prevents spam) — the server points the way, the client re-joins (separated rooms); **P1-11 safe zone + per-map cap (GS §14 · TA §6)**: maxClients = soloChannelCapacityForZone(map.zoneType) → safe (city) = cityHubCapacity (~80) / field = channelCapacity (env CITY_HUB_CAPACITY override) · handleCast passes map.zoneType → validateCast rejects safe_zone (no combat in the city); TODO: persistence, the anti-exploit death case (P2)
- `server/schema/MapRoomState.ts` — @colyseus/schema state: PlayerState{tx,ty,direction,anim,**partyId** P1-08} + **MobState{mobId,mobType,tx,ty,state,hp}** (hp updates for real, P1-05) + MapRoomState{mapId,channelId(server-assigned),**partyId** P1-08,roomId,players,**mobs**}
- `server/tsconfig.json` — the server's own separate tsconfig (legacy decorators, node env; separate from the Next tsconfig)
- `server/security/handshake.ts` — **pure** WS handshake decision logic (P2-04, Bible 5.2 + Storage §4): origin → token — **production always fails closed** (no token/secret = reject) · dev with no token = guest bypass (accountId=null, skips the lease — usable for e2e/local)
- `server/security/origin-allowlist.ts` + `server/security/rate-limiter.ts` — an allowlist from env `ALLOWED_ORIGINS` (empty=dev allows every origin+warns) · a sliding window of 10 failures/60s per IP (in-memory, TODO Redis)
- `server/security/session-registry.ts` + `server/security/session-lease.ts` — in-process takeover-wins (`WS_CLOSE_SESSION_TAKEN_OVER`=4001, kicks the old session without entering grace) · the lease in the DB is best-effort (no DB → skipped+warns once, **must never break a join**)
- `tests/server-security.test.ts` — 26 asserts: the handshake through every path (prod/dev × token/secret/origin) + the rate limiter + the takeover decision
- `server/characters/persistence-decision.ts` — **pure** (P2-05): pickLoadPosition (uses the saved position when mapId matches the room + it's walkable, otherwise the default) + pickSavePosition (always safe-valid) + the save-interval throttle decision
- `server/characters/character-state.ts` — CharacterState load/upsert + lastPlayedCharacterId (**best-effort** — no DB → the game keeps running in-memory, same pattern as session-lease)
- `src/engine/net/character-session.ts` + `src/app/hub/enter-game.ts` — read/write the selected characterId via sessionStorage (`SELECTED_CHARACTER_STORAGE_KEY` in net-protocol) → attached to joinOptions · the hub's Continue button = the entry point · **owner-report#6 fix**: also writes/reads the most recent mapId (`SELECTED_CHARACTER_MAP_STORAGE_KEY`) — `rememberSelectedCharacter(id,lastMapId)` (enter-game.ts) writes it when the hub's "enter game" fires, `readSelectedCharacterMapId`/`rememberSelectedCharacterMapId` (character-session.ts) read it on boot + overwrite it on every map crossing (app.ts); `pickBootMapId(stored,hasMap,defaultMapId)` = a **pure** decision picking the boot map · **owner-report round 2**: `clearSelectedCharacter`/`clearSelectedCharacterMapId` (best-effort removeItem) used by `src/app/game/boot-gate.ts` when it finds a characterId that's dead (tests: `tests/engine-net-character-session.test.ts`)
- `tests/server-characters-persistence.test.ts` — 10 cases: the load/save decision + ownership + the **transition-save invariant** (see known-traps)

## Auth (P2-03 — custom lightweight, decision-index 2026-07-12 · src/server/** = Next server-only)

- `src/server/db.ts` — the Prisma client singleton on the Next API side (separate from the realtime process's `server/db/client.ts`) — server-only, must never enter the client bundle
- `src/server/auth/signed-token.ts` — **pure** HS256 compact JWS on node:crypto (a pinned header to prevent alg-confusion, timingSafeEqual, an injectable nowSec) — used for both the session cookie (SESSION_SECRET) + the realtime token (JWT_SECRET)
- `src/server/auth/realtime-token.ts` + `src/server/auth/session-cookie.ts` — issue/verify a ~60s JWT (sub+jti, aud realtime → used by P2-04) · the session = a stateless httpOnly cookie
- `src/server/auth/password.ts` + `src/server/auth/password-policy.ts` + `src/server/auth/common-passwords.ts` — a scrypt hash (self-describing `scrypt$N$r$p$salt$hash` → can migrate to argon2id) + a policy + a blocklist
- `src/server/auth/email.ts` — normalize (lowercases the entire address = the uniqueness key, L-doc §1.3) + validate + double-entry confirm
- `src/server/auth/service.ts` + `src/server/auth/upgrade.ts` — the auth state machine: guest → register/login → **a guest upgrade keeps the same accountId** (idempotent, the reason never leaks whether the email already exists — prevents user enumeration)
- `src/server/auth/repository.ts` — the contract + EmailTakenError · `src/server/auth/memory-repository.ts` (for tests) · `src/server/auth/prisma-repository.ts` (the real one — P2002→EmailTakenError, upgrade = update the existing id + upgradedAt)
- `src/server/auth/http.ts` + `src/server/auth/secret.ts` — http utilities + reads secrets from env (throws if empty/too short)
- `src/app/api/auth/guest/route.ts` (+ `register`,`login`,`upgrade`,`session`,`rt-token` under `src/app/api/auth/`) — 6 endpoints: POST guest/register/login/upgrade · GET+DELETE session · POST rt-token
- `tests/server-auth-service.test.ts` (+ email/password-policy/password/token) — all pure unit tests running against the in-memory repo (**must never hit a real DB**)

## Characters + Game Hub (P2-06a — Storage §3–§9 · S4: hub = a route inside the existing app)

- `src/shared/character-name.ts` — **pure** character-name validator (Storage §3.3): NFC normalize + 3–16 code points + Thai/Latin/digits + at most 1 internal space — error codes empty/too_short/too_long/invalid_char/invalid_space · used by both the client (realtime feedback) + the server (authoritative)
- `src/shared/character-class.ts` — the CLASS_IDS whitelist (**currently just `swordsman`** — the English classId for the other 4 classes hasn't been decided yet, don't guess; added when P2B's archer arrives)
- `src/server/characters/` — repository contract + memory (tests) + prisma (P2002→name_taken) + service (the slot cap comes from account.characterSlots, a cross-account guard, duplicate classes are allowed) + an http helper — the same pattern as src/server/auth · **owner-report#6 fix**: `CharacterRecord`/`CharacterView` add `lastMapId: string|null` (from the CharacterState relation, prisma `include:{state:true}`; the memory repo/newly-created characters = always null) so the hub knows which map that character last saved on
- `src/app/api/characters/route.ts` — GET list / POST create (session-gated 401, `{ok:false,reason,nameError}`)
- `src/app/hub/page.tsx` + components in the same folder (`src/app/hub/HubShell.tsx`, AuthPanel, UpgradePanel, ContinueCard, CharacterGrid, CharacterCreate, messages) — **Game Hub**: guest/login/register → a Continue Card + a 5-slot table → the character-creation screen (realtime validation, 4 classes disabled as "coming soon") — uses the V1 token set (radius 6/10/16, hit area ≥48px) · the enter-game buttons (ContinueCard/CharacterGrid) = a link to /game + `rememberSelectedCharacter(id, lastMapId)` (remembers the characterId + latest map before navigating — **owner-report#6 fix**, see `src/engine/net/character-session.ts`) · the HubShell header: guests see a "Link Email" button (view="upgrade" → UpgradePanel, POST src/app/api/auth/upgrade/route.ts) + a "Log out" button for every account (DELETE src/app/api/auth/session/route.ts) — a guest who hasn't linked an email yet gets an inline confirm before logging out (a guest session is cookie-only, once cleared there's no way back into the same account)
- `tests/shared-character-name.test.ts` + `tests/server-characters-service.test.ts` — every validator error code + the full service path (memory repo, no DB hit) — including lastMapId in the view (owner-report#6 fix)
- `tests/engine-net-character-session.test.ts` — readSelectedCharacterMapId/rememberSelectedCharacterMapId (mocking sessionStorage via the window global) + pickBootMapId (pure) — owner-report#6 fix

## Persistence foundation (P2-02 — prisma/ + server/db/**)

- `prisma/schema.prisma` — **the first MySQL schema, 10 tables** (TA §7/§8 + AJ §4.3/§20, Prisma 6.x pinned — v7 is breaking): accounts (guest+email upgrade) · characters (**name @unique — pending owner: the rule for duplicate names**) · character_state (hot writes in a separate table) · items (id references config, doesn't store the definition) · inventory (version = an optimistic lock) · **currency_ledger (double-entry — no balance column, idempotencyKey unique, append-only)** · enhancement_logs · drop_audit (RNG audit) · config_versions (key+version+payload+active) · **game_events (append-only, eventId unique for dedup, indexed for retention — per decision A4)** — **a never-downgrade zone (DB schema)**
- `prisma/migrations/0001_init/migration.sql` — migration SQL generated offline via `prisma migrate diff --from-empty` (**has never been applied to any DB yet** — dev uses local MySQL, Hostinger = P2-16)
- `server/db/client.ts` — the Prisma client singleton (lazy init + an env guard that throws if DATABASE_URL is missing) — **server-only, must not be imported into src/engine|game|ui**
- `server/db/ledger.ts` — the ledger contract: getBalance = a raw SQL SUM · appendEntry = a skeleton that **throws until P2-08** (a transaction with FOR UPDATE + an idempotency insert)

## UI (React overlay)

- `src/ui/GameCanvas.tsx` — a "use client" bridge: mounts/unmounts the engine (guards against StrictMode double-mount); keeps the EngineHandle in a ref (not React state) + renders `<DebugOverlay>` (P0-11) · **owner-report round 2**: calls `resolveGameEntry` (boot-gate.ts) before `createEngine` every time (inside the same setTimeout(0), awaits then checks cancelled) — a redirect-hub result → router.replace to /hub (next/navigation), a mount result → createEngine as before
- `src/ui/DebugOverlay.tsx` — "use client" (P0-11 → **P2-01 moved into Zustand**): a top-right panel subscribing to `useGameStore(selectDebugInfo)` (replacing the old poll) showing fps/player tile/pointer tile/entityCount/net(status·mapId·roomId·channelId·party·playerCount) + a toggle button for depth debug (the imperative command still goes through the EngineHandle accessor) + show/hide (button + F3)
- `src/ui/debug-overlay-logic.ts` — **pure** (P0-11): DebugOverlayState + `isDebugToggleKey`/`toggleVisible`/`toggleDepthDebug` (a reducer, kept separate from the component so it's testable without rendering)
- `src/ui/store/game-store.ts` — the **Zustand vanilla store bridge** (P2-01, contract in `docs/context/ui.md`): HudState + the gameStore singleton + `createHudPublisher` (throttled ~4Hz, builds the thunk only when it's actually queued, clock/writer injectable = testable) + `resetHudState` (engine teardown/StrictMode) — **must not import React in this file** (the engine's `src/engine/runtime/app.ts` imports it directly to publish)
- `src/ui/store/use-game-store.ts` — the "use client" React hook `useGameStore(selector)` wrapping the vanilla gameStore (deliberately a separate file from the vanilla one — components should only import this one)

## E2E harness (P2-00 — scripts/e2e/, permanent, works both local/prod)

- `scripts/e2e/lib.mjs` — shared helpers (plain Node ESM, doesn't use tsx): connect (the Colyseus client + joinOptions), waitFor (poll+timeout+a clear failure message), report (summary+exit code), the url from env `E2E_RT_URL` (default ws://localhost:2567)
- `scripts/e2e/smoke.mjs` — the main 8-step scenario (~30s): join+ROOM_STATE → **adopt self's position before the first move** (trap P1) → walk at 12Hz to pocket-slime-south → assert 0 corrections → find a mob+cast → assert MSG_SKILL_RESULT · note: constants (MSG_*, speed, sync Hz, spawn, collision) are **copied from config/net-protocol with line-number comments** — .mjs can't import .ts; if you change config/protocol, you must also update this file
- `scripts/e2e/README.md` — how to run it locally/on prod (`npm run e2e` or `E2E_RT_URL=wss://... node scripts/e2e/smoke.mjs`) + cold-start caveats

## Config

- `package.json` — scripts + dependencies (npm) — the `e2e` script = `node scripts/e2e/smoke.mjs`
- `next.config.ts` — Next.js config
- `tsconfig.json` — TypeScript config (alias `@/*` → `src/*`)
- `eslint.config.mjs` — ESLint flat config
- `postcss.config.mjs` — PostCSS (Tailwind v4)
- `vitest.config.ts` — Vitest config

## Tests

- `tests/docs-guard.test.ts` — path-guard: every file referenced in CODEMAP/feature-map/context must actually exist
- `tests/db-schema.test.ts` — a schema-level guard (P2-02, doesn't need a real DB — reads the schema/migration and asserts): all 10 tables exist + currency_ledger has **no** balance column + idempotency_key/event_id are unique + inventory.version is an optimistic lock + game_events is fully indexed
- `tests/engine-config.test.ts` — EngineConfig defaults / merge / resolveResolution
- `tests/engine-resize.test.ts` — clampSize (the pure resize helper)
- `tests/engine-iso-coords.test.ts` — iso converters: known values / round-trip fuzz / snapToTile
- `tests/engine-iso-depth.test.ts` — depthKey ordering + zLayer band non-overlap
- `tests/engine-map-loader.test.ts` — map loader validation (invariants throw) + P0 Test Field passes + **P1-07** safeCamp optional (not specified→safeCampOf=spawnPoint · specified and walkable passes · overlapping collision/out of bounds→throws); **P1-10**: exits optional (parse/duplicate id/area out of bounds/empty targetMapId/non-finite targetSpawn) + findExitAt/isTileInRect; **P1-11**: zoneType optional (default field/specifying safe·field passes/an invalid enum→throws)
- `tests/engine-map-exit-reachability.test.ts` — **P1 fix** guard: for every map in MAP_REGISTRY — the spawnPoint can actually reach the exit area (findPath ≠ null to at least one walkable tile in every exit) + every exit area has at least one walkable tile (not fully blocked) — prevents placing an exit behind a wall
- `tests/engine-map-registry.test.ts` — **P1-10/P1-11** registry: MAP_REGISTRY has 3 maps (+ city-hub) + getMap/requireMap/hasMap · topology testfield→map1, map1↔city-hub (targetSpawn is walkable + outside the destination exit) · CITY_HUB passes validation (32×32/zone safe/0 pockets/1 exit/locked name) · MAP1 (40×40/4 pockets/§6 density) · buildMapRegistry cross-refs (duplicate mapId/a phantom targetMapId/an unwalkable targetSpawn → throws)
- `tests/engine-runtime-transition-state.test.ts` — **P1-10** the transition state machine (pure): idle→fadeOut alpha climbs 0→1 → fireSwap fires exactly once at full darkness → fadeIn → idle · locked throughout any non-idle state · duration 0 = instant but still swaps exactly once
- `tests/engine-render-depth.test.ts` — DepthRegistry: sorted order by depthKey + tie-breaking + dirty tracking + lifecycle
- `tests/engine-render-camera.test.ts` — camera math: map screen bounds (square/non-square) + clamping on all 4 edges + content==viewport + lerp follow + **P1-06**: applyShakeOffset (offset {0,0} is a no-op + adds directly on both axes)
- `tests/engine-render-placement.test.ts` — locks in the foot convention: entityFootToScreen = tileToScreen (no +0.5), ≠ tileCenterToScreen
- `tests/engine-input-keyboard.test.ts` — intent mapping screen→tile (W/W+D/all released/opposite keys cancel out) + the basis projects in the correct direction
- `tests/engine-movement-mover.test.ts` — stepMovement: straight/diagonal normalize + dt clamp + axis-separated collision slide + blocked stops
- `tests/engine-movement-direction.test.ts` — resolveDirection: 8 combos→direction + all 45° angles + the 22.5° boundary + idle keeps the current direction + directionToScreenUnit
- `tests/engine-pathfinding-astar.test.ts` — findPath (P1-09): ortho/diagonal open paths + float snapping + routing around walls (avoids blocked, stays continuous) + null (goal surrounded/goal blocked/outside bounds) + prevents corner cutting (can't cut through a wall corner, can cut diagonally through open corners) + the node cap (a distant goal with a low cap→null, a sufficient cap→found)
- `tests/engine-movement-path-follower.test.ts` — advancePathFollower (P1-09): reaching a waypoint (arrivalRadius) + multiple waypoints + heading points at the target + blocked (target blocked / stuck against an intervening wall) + cancellation (empty waypoints/index past the end → arrived, doesn't move)
- `tests/engine-animation-manifest.test.ts` — resolveClip: 5 drawn directions without mirroring + 3 mirrored ones point at the right source + all 8 directions×idle/walk covered + the 8-dir override + errors (unknown anim / a direction with no clip / a mirror source that isn't drawn) + advancePlayhead timing/loop/clamp/guard
- `tests/engine-net-sync.test.ts` — net sync pure logic (P0-07): coerce dir/anim + snapshotChanged + advanceSendTimer throttle/clamp + toMoveMessage + shared protocol constants + **P1-08** the JoinOptions partyId shape (channelId removed) + channelLabel + computePlayerCount
- `tests/engine-net-party.test.ts` — resolvePartyId pure (P1-08 §59.3): ?party=xyz → partyId · missing/empty/whitespace → fallback · trim · a query without the `?` prefix · 2 clients with the same party = they match
- `tests/server-channel-registry.test.ts` — the channel registry pure logic (P1-08 §59.3): assigns CH.1/2/3 in order · channelNumber matches the label · release→reuses the lowest free number · a per-map pool is independent · releasing an invalid format/unknown value = a no-op · channelNumbersFor is sorted
- `tests/engine-net-interpolation.test.ts` — the interpolation buffer's pure logic (P1-01): lerps at the midpoint/proportionally + direction/anim come from the newer snapshot + edge cases: empty→null/a single snapshot/a newly-created entity clamps + short extrapolation+clamped at the max + out-of-order/duplicate dropped + ring overflow + pool slot reuse
- `tests/shared-reconnect.test.ts` — reconnect pure logic (P1-07, §59.1): decideReconnect's decision table (within/expired/room closed/position invalid) + resolveSpawnPosition (walkable→requested · overlapping collision/non-finite→safe camp · pos is a copy) + reconnectBackoffMs (order+cap+negative/float attempt) + cumulative backoff < grace + shouldRetryReconnect; **P1-07-fix**: planRejoin (a fresh token→reconnect · missing/empty/past grace/a different server·map·party/age<0→fresh) + parseStoredReconnect (a complete record→returned · non-object/missing field/wrong type→null)
- `tests/engine-net-reconnect-store.test.ts` — **P1-07-fix**: the reconnect-store adapter — a memory store roundtrip + a storage store (a mocked KeyValueStorage): save writes JSON/load parses it/clear removes the key · corrupt JSON/an incomplete schema→null · a throwing storage (private mode/quota)→a no-op, doesn't propagate
- `tests/shared-movement-validation.test.ts` — validateMove pure logic (P1-02): normal walking passes (multiple speeds/elapsed values) + speed hacks caught (reason speed) + blocked caught (correctTo=prev) + teleport caught (the absolute cap, before speed) + elapsed 0/negative edge cases (clock skew, no divide-by-zero) + maxElapsed clamp (a long gap doesn't inflate the allowance) + non_finite + jitter tolerance doesn't false-positive
- `tests/game-mob-rng.test.ts` — createLcgRng: the same seed→the same sequence, different seeds→different sequences, values stay in [0,1); a smoke test for defaultRng
- `tests/game-mob-spawn.test.ts` — spawnPocketMobs/spawnAllPockets (pure): the count stays within packSize's range + clamped to activeCap (multiple seeds) + deterministic per seed + spawn points land inside pocket.area and are actually walkable (never on blocked tiles) + findWalkableSpawnPoint returns undefined when nothing is found (never throws) + real P0_TEST_FIELD (3 pockets don't overlap)
- `tests/game-mob-wander.test.ts` — createWanderState/stepWander (pure): idle/walk alternate per config (never hardcoded) + distance walked = speed·dt + pure (no mutation) + the leash never leaves pocket.area (deterministic + a seeded random over hundreds of steps) + the leash also respects the real map's collision + walkableFromMap wires up isWalkableTile correctly
- `tests/game-mob-ai.test.ts` — mob AI pure logic (P1-03): selectAggroTarget (nearest within radius/at the edge/skipping players at the full pull cap) + shouldReturnToSpawn (null/leash/deaggro) + hasReachedSpawn + stepToward (speed·dt/normalize/sliding when blocked) + isPocketActive (AOI rect edge/inside the pocket) + idleTickInterval/shouldStepPocket (LOD active/idle/asleep) + isRespawnDue (clock)
- `tests/game-mob-simulation.test.ts` — createMobSimulation (P1-03, injects rng/clock): the initial spawn + aggro enter/chase (distance shrinks) + the pull cap (more mobs than cap → aggro ≤ cap) + leash/return (luring it out of the pocket → it flees far → returns → back inside pocket.area) + the respawn timer (kill→due→respawns, never exceeding activeCap) + AI LOD asleep (no players + idleTickHz 0 → frozen)
- `tests/game-skill-loader.test.ts` — the skill schema loader (P1-04, GS §50.1 · TA §16.1): SKILL_FIELD_NAMES has all 37, matching name/order + loadSkillDefinitions passes on good input/a duplicate skillId→throws/each of the 37 fields missing→throws naming it/a wrong type (string/boolean/array)→throws/an invalid negative value (cooldown/range/maxTargets/unlockLevel/baseMultiplier)→throws/an unknown field (including typos)→throws naming it + all 4 WARRIOR_SKILLS pass real validation + serverView has all 37/is a fresh copy + clientView strips all 9 server-only fields for every skill
- `tests/game-combat-formula.test.ts` — the damage formula (pure, P1-05 + P2-14, §15.2/§50.1.1): effectiveDef floors at 0 + mitigationFactor (DEF=k→0.5/DEF=0→1.0/the proposal §1.3 table/guards div-by-0) + computeDamage (DEF=k halves it, Penetration, the crit path, bossMod/pvpMod/tierReduction, never negative, hand-calculated §1.4 slime 24/mushroom 22) + computeSkillDamage round-once (a non-divisible case, deterministic, non-negative, subHits sum===damage, a property matrix of 4 base × 3 crit-modes × hitCount {2,3,5,7}, multi-hit crit, hitCount 0)
- `tests/server-cast-validation.test.ts` — cast validation (pure, P1-05/P1-11, §16.2/§16.3/§18.4 · §14): cooldown (isSkillReady/skillReadyAt) + range (isAimInRange) + skillAttackShape (cone/circle/null) + resolveSkillHits (the maxTargets cap, nearest-first, nothing outside the radius gets hit) + validateCast (safe_zone/unknown_skill/cooldown/out_of_range in that order — **P1-11** safe zone beats every other reason)
- `tests/game-combat-hit-test.test.ts` — findHits (pure, P0-10): range+arc around the facing direction (8 directions, including a guard against a half-arc >45° with a near-tie boundary tested separately), nothing behind the player gets hit, distance 0 always hits + rollDummyDamage stays in range (seeded RNG, deterministic) + advanceCooldown/canAttack (the cooldown gate over a full cycle) + applyDummyDamage (the hp/death transition)
- `tests/engine-runtime-debug-info.test.ts` — roundTile (rounds to 2 decimal places) + buildDebugInfo's shape (fps rounded, pointerTile null-safe) + IDLE_NET_DEBUG_INFO (P0-11, pure)
- `tests/ui-debug-overlay-logic.test.ts` — isDebugToggleKey (F3, ignores other keys) + the toggleVisible/toggleDepthDebug reducer (pure, no mutation) (P0-11)
- `tests/engine-render-object-pool.test.ts` — createObjectPool (pure, P1-06, TA §11): acquire builds up to capacity/exceeding the cap returns undefined (no extra factory calls)/release+acquire reuses the same object (zero-alloc)/releasing a foreign object+double-release = a safe no-op (doesn't corrupt the free-list)/capacity ≤0 throws
- `tests/engine-render-screen-shake.test.ts` — screen-shake pure state (P1-06, GS §17.5): trigger sets amplitude×scale/duration + a level beyond the array clamps + a stronger shake replaces/a weaker one doesn't override + advanceShake real-time decay + computeShakeOffset's linear decay × a direction from the rng (deterministic) + expired→{0,0} + an empty array = a no-op
- `tests/game-combat-damage-aggregate.test.ts` — the damage-aggregate window (pure, P1-06, GS §17.10): a single hit doesn't flush until the window completes + accumulating for a full windowMs → flushes the total/hitCount/latest tile + flushing clears the bucket (a new window starts counting fresh) + multiple buckets stay independent + anyCrit = OR across every hit
- `tests/game-combat-hit-stop.test.ts` — hit-stop pure state/timescale (P1-06, GS §17.5): trigger sets remainingMs per level + a level beyond bounds/negative clamps + stacking uses the larger value (never adds up) + advanceHitStop real-time decay + an empty array = duration 0
- `tests/game-combat-stress-harness-rate.test.ts` — computeStressSpawnBatch (pure, P1-06 §5): rate/dt lining up exactly → 1 spawn + a high rate (300/s) ≈ 4-5 times/frame at 60fps + accumulating across multiple frames ≈ the expected rate (deterministic) + maxSpawnPerTick caps spikes when dt jumps + rate ≤0 = off + a dt smaller than the interval = 0 spawns
- `tests/game-combat-juice-level.test.ts` — resolveJuiceLevel (pure, P1-06 follow-up, GS §17.5): a high enough baseLevel isn't floored + killed raises it to minLevelOnKill + crit raises it to minLevelOnCrit + a baseLevel above the floor is never reduced + killed+crit uses the higher floor + neither killed nor crit → no floor at all + floor=0 = the feature is off
- `tests/game-combat-target-engage.test.ts` — the target-engage state machine (pure, P1-09.1, TA §17.3): idle→action is always none + engaging within range→attacks continuously (the state never ends) + out of range with no existing path→chase + out of range but already has a path→none (doesn't replan again) + a null target (dead)→idle + a target id mismatch→idle + exactly at attackRange→attack (`<=`) + cancelEngage/startEngage

## Docs

- `AI.md` — the universal agent entry point
- `CLAUDE.md` — the orchestrator entry point
- `AGENTS.md` — framework traps (Next.js 16)
- `docs/README.md` — the docs table of contents
- `docs/current-state.md` — live state
- `docs/decision-index.md` — locked decisions
- `docs/known-traps.md` — bug classes
- `docs/feature-map.md` — feature → spec/source/tests
- `docs/token-budget.md` — the reading budget
- `docs/context/engine.md` — the engine context pack
- `docs/context/ui.md` — the ui context pack
- `docs/design/` — the game spec (v15 canonical + the P0 scope lock + map bibles)
- `docs/design/art-reference/` — reference images from the owner (the visual north star) + an index README
- `docs/design/proposals/` — **proposals awaiting an owner decision (not spec)** · `docs/design/proposals/deungpu_P1_BALANCE_PROPOSAL_v1.md` = the P1 balance draft (k value, stat baselines, 5-class skills §50.1, Map 1 mobs) — PENDING OWNER, enters spec via §59.4
- `docs/tech/` — the tech spec (architecture v1.5 + decision locks)
